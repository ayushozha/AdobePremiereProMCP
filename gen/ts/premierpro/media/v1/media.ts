// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: premierpro/media/v1/media.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Asset, Resolution, Timecode } from "../../common/v1/common";

export const protobufPackage = "premierpro.media.v1";

export interface ScanAssetsRequest {
  /** Root directory to scan. */
  directory: string;
  /** Scan subdirectories recursively. */
  recursive: boolean;
  /** File extension filters (e.g., [".mp4", ".mov"]). Empty = all media. */
  extensions: string[];
}

export interface ScanAssetsResponse {
  assets: Asset[];
  totalFilesScanned: number;
  mediaFilesFound: number;
  scanDurationSeconds: number;
}

export interface ProbeMediaRequest {
  filePath: string;
}

export interface ProbeMediaResponse {
  asset: Asset | undefined;
}

export interface GenerateThumbnailRequest {
  filePath: string;
  timestamp: Timecode | undefined;
  outputSize:
    | Resolution
    | undefined;
  /** "png", "jpg" */
  outputFormat: string;
}

export interface GenerateThumbnailResponse {
  thumbnailData: Uint8Array;
  outputPath: string;
  actualSize: Resolution | undefined;
}

export interface AnalyzeWaveformRequest {
  filePath: string;
  audioTrack: number;
  /** e.g., -40.0 */
  silenceThresholdDb: number;
  /** e.g., 0.5 */
  minSilenceDurationSeconds: number;
}

export interface AnalyzeWaveformResponse {
  silenceRegions: SilenceRegion[];
  peakDb: number;
  rmsDb: number;
  durationSeconds: number;
  /** downsampled waveform */
  waveformSamples: number[];
}

export interface SilenceRegion {
  startSeconds: number;
  endSeconds: number;
  avgDb: number;
}

export interface DetectScenesRequest {
  filePath: string;
  /** 0.0-1.0, sensitivity of scene detection. */
  threshold: number;
}

export interface DetectScenesResponse {
  scenes: SceneChange[];
}

export interface SceneChange {
  timecode: Timecode | undefined;
  confidence: number;
}

function createBaseScanAssetsRequest(): ScanAssetsRequest {
  return { directory: "", recursive: false, extensions: [] };
}

export const ScanAssetsRequest: MessageFns<ScanAssetsRequest> = {
  encode(message: ScanAssetsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.directory !== "") {
      writer.uint32(10).string(message.directory);
    }
    if (message.recursive !== false) {
      writer.uint32(16).bool(message.recursive);
    }
    for (const v of message.extensions) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanAssetsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanAssetsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.directory = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.recursive = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extensions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanAssetsRequest {
    return {
      directory: isSet(object.directory) ? globalThis.String(object.directory) : "",
      recursive: isSet(object.recursive) ? globalThis.Boolean(object.recursive) : false,
      extensions: globalThis.Array.isArray(object?.extensions)
        ? object.extensions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ScanAssetsRequest): unknown {
    const obj: any = {};
    if (message.directory !== "") {
      obj.directory = message.directory;
    }
    if (message.recursive !== false) {
      obj.recursive = message.recursive;
    }
    if (message.extensions?.length) {
      obj.extensions = message.extensions;
    }
    return obj;
  },

  create(base?: DeepPartial<ScanAssetsRequest>): ScanAssetsRequest {
    return ScanAssetsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScanAssetsRequest>): ScanAssetsRequest {
    const message = createBaseScanAssetsRequest();
    message.directory = object.directory ?? "";
    message.recursive = object.recursive ?? false;
    message.extensions = object.extensions?.map((e) => e) || [];
    return message;
  },
};

function createBaseScanAssetsResponse(): ScanAssetsResponse {
  return { assets: [], totalFilesScanned: 0, mediaFilesFound: 0, scanDurationSeconds: 0 };
}

export const ScanAssetsResponse: MessageFns<ScanAssetsResponse> = {
  encode(message: ScanAssetsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.assets) {
      Asset.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalFilesScanned !== 0) {
      writer.uint32(16).uint32(message.totalFilesScanned);
    }
    if (message.mediaFilesFound !== 0) {
      writer.uint32(24).uint32(message.mediaFilesFound);
    }
    if (message.scanDurationSeconds !== 0) {
      writer.uint32(33).double(message.scanDurationSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScanAssetsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScanAssetsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assets.push(Asset.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalFilesScanned = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.mediaFilesFound = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.scanDurationSeconds = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScanAssetsResponse {
    return {
      assets: globalThis.Array.isArray(object?.assets) ? object.assets.map((e: any) => Asset.fromJSON(e)) : [],
      totalFilesScanned: isSet(object.totalFilesScanned)
        ? globalThis.Number(object.totalFilesScanned)
        : isSet(object.total_files_scanned)
        ? globalThis.Number(object.total_files_scanned)
        : 0,
      mediaFilesFound: isSet(object.mediaFilesFound)
        ? globalThis.Number(object.mediaFilesFound)
        : isSet(object.media_files_found)
        ? globalThis.Number(object.media_files_found)
        : 0,
      scanDurationSeconds: isSet(object.scanDurationSeconds)
        ? globalThis.Number(object.scanDurationSeconds)
        : isSet(object.scan_duration_seconds)
        ? globalThis.Number(object.scan_duration_seconds)
        : 0,
    };
  },

  toJSON(message: ScanAssetsResponse): unknown {
    const obj: any = {};
    if (message.assets?.length) {
      obj.assets = message.assets.map((e) => Asset.toJSON(e));
    }
    if (message.totalFilesScanned !== 0) {
      obj.totalFilesScanned = Math.round(message.totalFilesScanned);
    }
    if (message.mediaFilesFound !== 0) {
      obj.mediaFilesFound = Math.round(message.mediaFilesFound);
    }
    if (message.scanDurationSeconds !== 0) {
      obj.scanDurationSeconds = message.scanDurationSeconds;
    }
    return obj;
  },

  create(base?: DeepPartial<ScanAssetsResponse>): ScanAssetsResponse {
    return ScanAssetsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScanAssetsResponse>): ScanAssetsResponse {
    const message = createBaseScanAssetsResponse();
    message.assets = object.assets?.map((e) => Asset.fromPartial(e)) || [];
    message.totalFilesScanned = object.totalFilesScanned ?? 0;
    message.mediaFilesFound = object.mediaFilesFound ?? 0;
    message.scanDurationSeconds = object.scanDurationSeconds ?? 0;
    return message;
  },
};

function createBaseProbeMediaRequest(): ProbeMediaRequest {
  return { filePath: "" };
}

export const ProbeMediaRequest: MessageFns<ProbeMediaRequest> = {
  encode(message: ProbeMediaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filePath !== "") {
      writer.uint32(10).string(message.filePath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProbeMediaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProbeMediaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProbeMediaRequest {
    return {
      filePath: isSet(object.filePath)
        ? globalThis.String(object.filePath)
        : isSet(object.file_path)
        ? globalThis.String(object.file_path)
        : "",
    };
  },

  toJSON(message: ProbeMediaRequest): unknown {
    const obj: any = {};
    if (message.filePath !== "") {
      obj.filePath = message.filePath;
    }
    return obj;
  },

  create(base?: DeepPartial<ProbeMediaRequest>): ProbeMediaRequest {
    return ProbeMediaRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProbeMediaRequest>): ProbeMediaRequest {
    const message = createBaseProbeMediaRequest();
    message.filePath = object.filePath ?? "";
    return message;
  },
};

function createBaseProbeMediaResponse(): ProbeMediaResponse {
  return { asset: undefined };
}

export const ProbeMediaResponse: MessageFns<ProbeMediaResponse> = {
  encode(message: ProbeMediaResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.asset !== undefined) {
      Asset.encode(message.asset, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProbeMediaResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProbeMediaResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.asset = Asset.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProbeMediaResponse {
    return { asset: isSet(object.asset) ? Asset.fromJSON(object.asset) : undefined };
  },

  toJSON(message: ProbeMediaResponse): unknown {
    const obj: any = {};
    if (message.asset !== undefined) {
      obj.asset = Asset.toJSON(message.asset);
    }
    return obj;
  },

  create(base?: DeepPartial<ProbeMediaResponse>): ProbeMediaResponse {
    return ProbeMediaResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProbeMediaResponse>): ProbeMediaResponse {
    const message = createBaseProbeMediaResponse();
    message.asset = (object.asset !== undefined && object.asset !== null) ? Asset.fromPartial(object.asset) : undefined;
    return message;
  },
};

function createBaseGenerateThumbnailRequest(): GenerateThumbnailRequest {
  return { filePath: "", timestamp: undefined, outputSize: undefined, outputFormat: "" };
}

export const GenerateThumbnailRequest: MessageFns<GenerateThumbnailRequest> = {
  encode(message: GenerateThumbnailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filePath !== "") {
      writer.uint32(10).string(message.filePath);
    }
    if (message.timestamp !== undefined) {
      Timecode.encode(message.timestamp, writer.uint32(18).fork()).join();
    }
    if (message.outputSize !== undefined) {
      Resolution.encode(message.outputSize, writer.uint32(26).fork()).join();
    }
    if (message.outputFormat !== "") {
      writer.uint32(34).string(message.outputFormat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateThumbnailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateThumbnailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = Timecode.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.outputSize = Resolution.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.outputFormat = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateThumbnailRequest {
    return {
      filePath: isSet(object.filePath)
        ? globalThis.String(object.filePath)
        : isSet(object.file_path)
        ? globalThis.String(object.file_path)
        : "",
      timestamp: isSet(object.timestamp) ? Timecode.fromJSON(object.timestamp) : undefined,
      outputSize: isSet(object.outputSize)
        ? Resolution.fromJSON(object.outputSize)
        : isSet(object.output_size)
        ? Resolution.fromJSON(object.output_size)
        : undefined,
      outputFormat: isSet(object.outputFormat)
        ? globalThis.String(object.outputFormat)
        : isSet(object.output_format)
        ? globalThis.String(object.output_format)
        : "",
    };
  },

  toJSON(message: GenerateThumbnailRequest): unknown {
    const obj: any = {};
    if (message.filePath !== "") {
      obj.filePath = message.filePath;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = Timecode.toJSON(message.timestamp);
    }
    if (message.outputSize !== undefined) {
      obj.outputSize = Resolution.toJSON(message.outputSize);
    }
    if (message.outputFormat !== "") {
      obj.outputFormat = message.outputFormat;
    }
    return obj;
  },

  create(base?: DeepPartial<GenerateThumbnailRequest>): GenerateThumbnailRequest {
    return GenerateThumbnailRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenerateThumbnailRequest>): GenerateThumbnailRequest {
    const message = createBaseGenerateThumbnailRequest();
    message.filePath = object.filePath ?? "";
    message.timestamp = (object.timestamp !== undefined && object.timestamp !== null)
      ? Timecode.fromPartial(object.timestamp)
      : undefined;
    message.outputSize = (object.outputSize !== undefined && object.outputSize !== null)
      ? Resolution.fromPartial(object.outputSize)
      : undefined;
    message.outputFormat = object.outputFormat ?? "";
    return message;
  },
};

function createBaseGenerateThumbnailResponse(): GenerateThumbnailResponse {
  return { thumbnailData: new Uint8Array(0), outputPath: "", actualSize: undefined };
}

export const GenerateThumbnailResponse: MessageFns<GenerateThumbnailResponse> = {
  encode(message: GenerateThumbnailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.thumbnailData.length !== 0) {
      writer.uint32(10).bytes(message.thumbnailData);
    }
    if (message.outputPath !== "") {
      writer.uint32(18).string(message.outputPath);
    }
    if (message.actualSize !== undefined) {
      Resolution.encode(message.actualSize, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateThumbnailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateThumbnailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.thumbnailData = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputPath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.actualSize = Resolution.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateThumbnailResponse {
    return {
      thumbnailData: isSet(object.thumbnailData)
        ? bytesFromBase64(object.thumbnailData)
        : isSet(object.thumbnail_data)
        ? bytesFromBase64(object.thumbnail_data)
        : new Uint8Array(0),
      outputPath: isSet(object.outputPath)
        ? globalThis.String(object.outputPath)
        : isSet(object.output_path)
        ? globalThis.String(object.output_path)
        : "",
      actualSize: isSet(object.actualSize)
        ? Resolution.fromJSON(object.actualSize)
        : isSet(object.actual_size)
        ? Resolution.fromJSON(object.actual_size)
        : undefined,
    };
  },

  toJSON(message: GenerateThumbnailResponse): unknown {
    const obj: any = {};
    if (message.thumbnailData.length !== 0) {
      obj.thumbnailData = base64FromBytes(message.thumbnailData);
    }
    if (message.outputPath !== "") {
      obj.outputPath = message.outputPath;
    }
    if (message.actualSize !== undefined) {
      obj.actualSize = Resolution.toJSON(message.actualSize);
    }
    return obj;
  },

  create(base?: DeepPartial<GenerateThumbnailResponse>): GenerateThumbnailResponse {
    return GenerateThumbnailResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenerateThumbnailResponse>): GenerateThumbnailResponse {
    const message = createBaseGenerateThumbnailResponse();
    message.thumbnailData = object.thumbnailData ?? new Uint8Array(0);
    message.outputPath = object.outputPath ?? "";
    message.actualSize = (object.actualSize !== undefined && object.actualSize !== null)
      ? Resolution.fromPartial(object.actualSize)
      : undefined;
    return message;
  },
};

function createBaseAnalyzeWaveformRequest(): AnalyzeWaveformRequest {
  return { filePath: "", audioTrack: 0, silenceThresholdDb: 0, minSilenceDurationSeconds: 0 };
}

export const AnalyzeWaveformRequest: MessageFns<AnalyzeWaveformRequest> = {
  encode(message: AnalyzeWaveformRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filePath !== "") {
      writer.uint32(10).string(message.filePath);
    }
    if (message.audioTrack !== 0) {
      writer.uint32(16).uint32(message.audioTrack);
    }
    if (message.silenceThresholdDb !== 0) {
      writer.uint32(25).double(message.silenceThresholdDb);
    }
    if (message.minSilenceDurationSeconds !== 0) {
      writer.uint32(33).double(message.minSilenceDurationSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeWaveformRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyzeWaveformRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.audioTrack = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.silenceThresholdDb = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.minSilenceDurationSeconds = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyzeWaveformRequest {
    return {
      filePath: isSet(object.filePath)
        ? globalThis.String(object.filePath)
        : isSet(object.file_path)
        ? globalThis.String(object.file_path)
        : "",
      audioTrack: isSet(object.audioTrack)
        ? globalThis.Number(object.audioTrack)
        : isSet(object.audio_track)
        ? globalThis.Number(object.audio_track)
        : 0,
      silenceThresholdDb: isSet(object.silenceThresholdDb)
        ? globalThis.Number(object.silenceThresholdDb)
        : isSet(object.silence_threshold_db)
        ? globalThis.Number(object.silence_threshold_db)
        : 0,
      minSilenceDurationSeconds: isSet(object.minSilenceDurationSeconds)
        ? globalThis.Number(object.minSilenceDurationSeconds)
        : isSet(object.min_silence_duration_seconds)
        ? globalThis.Number(object.min_silence_duration_seconds)
        : 0,
    };
  },

  toJSON(message: AnalyzeWaveformRequest): unknown {
    const obj: any = {};
    if (message.filePath !== "") {
      obj.filePath = message.filePath;
    }
    if (message.audioTrack !== 0) {
      obj.audioTrack = Math.round(message.audioTrack);
    }
    if (message.silenceThresholdDb !== 0) {
      obj.silenceThresholdDb = message.silenceThresholdDb;
    }
    if (message.minSilenceDurationSeconds !== 0) {
      obj.minSilenceDurationSeconds = message.minSilenceDurationSeconds;
    }
    return obj;
  },

  create(base?: DeepPartial<AnalyzeWaveformRequest>): AnalyzeWaveformRequest {
    return AnalyzeWaveformRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalyzeWaveformRequest>): AnalyzeWaveformRequest {
    const message = createBaseAnalyzeWaveformRequest();
    message.filePath = object.filePath ?? "";
    message.audioTrack = object.audioTrack ?? 0;
    message.silenceThresholdDb = object.silenceThresholdDb ?? 0;
    message.minSilenceDurationSeconds = object.minSilenceDurationSeconds ?? 0;
    return message;
  },
};

function createBaseAnalyzeWaveformResponse(): AnalyzeWaveformResponse {
  return { silenceRegions: [], peakDb: 0, rmsDb: 0, durationSeconds: 0, waveformSamples: [] };
}

export const AnalyzeWaveformResponse: MessageFns<AnalyzeWaveformResponse> = {
  encode(message: AnalyzeWaveformResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.silenceRegions) {
      SilenceRegion.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.peakDb !== 0) {
      writer.uint32(17).double(message.peakDb);
    }
    if (message.rmsDb !== 0) {
      writer.uint32(25).double(message.rmsDb);
    }
    if (message.durationSeconds !== 0) {
      writer.uint32(33).double(message.durationSeconds);
    }
    writer.uint32(42).fork();
    for (const v of message.waveformSamples) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeWaveformResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyzeWaveformResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.silenceRegions.push(SilenceRegion.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.peakDb = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.rmsDb = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.durationSeconds = reader.double();
          continue;
        }
        case 5: {
          if (tag === 45) {
            message.waveformSamples.push(reader.float());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.waveformSamples.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyzeWaveformResponse {
    return {
      silenceRegions: globalThis.Array.isArray(object?.silenceRegions)
        ? object.silenceRegions.map((e: any) => SilenceRegion.fromJSON(e))
        : globalThis.Array.isArray(object?.silence_regions)
        ? object.silence_regions.map((e: any) => SilenceRegion.fromJSON(e))
        : [],
      peakDb: isSet(object.peakDb)
        ? globalThis.Number(object.peakDb)
        : isSet(object.peak_db)
        ? globalThis.Number(object.peak_db)
        : 0,
      rmsDb: isSet(object.rmsDb)
        ? globalThis.Number(object.rmsDb)
        : isSet(object.rms_db)
        ? globalThis.Number(object.rms_db)
        : 0,
      durationSeconds: isSet(object.durationSeconds)
        ? globalThis.Number(object.durationSeconds)
        : isSet(object.duration_seconds)
        ? globalThis.Number(object.duration_seconds)
        : 0,
      waveformSamples: globalThis.Array.isArray(object?.waveformSamples)
        ? object.waveformSamples.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.waveform_samples)
        ? object.waveform_samples.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: AnalyzeWaveformResponse): unknown {
    const obj: any = {};
    if (message.silenceRegions?.length) {
      obj.silenceRegions = message.silenceRegions.map((e) => SilenceRegion.toJSON(e));
    }
    if (message.peakDb !== 0) {
      obj.peakDb = message.peakDb;
    }
    if (message.rmsDb !== 0) {
      obj.rmsDb = message.rmsDb;
    }
    if (message.durationSeconds !== 0) {
      obj.durationSeconds = message.durationSeconds;
    }
    if (message.waveformSamples?.length) {
      obj.waveformSamples = message.waveformSamples;
    }
    return obj;
  },

  create(base?: DeepPartial<AnalyzeWaveformResponse>): AnalyzeWaveformResponse {
    return AnalyzeWaveformResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalyzeWaveformResponse>): AnalyzeWaveformResponse {
    const message = createBaseAnalyzeWaveformResponse();
    message.silenceRegions = object.silenceRegions?.map((e) => SilenceRegion.fromPartial(e)) || [];
    message.peakDb = object.peakDb ?? 0;
    message.rmsDb = object.rmsDb ?? 0;
    message.durationSeconds = object.durationSeconds ?? 0;
    message.waveformSamples = object.waveformSamples?.map((e) => e) || [];
    return message;
  },
};

function createBaseSilenceRegion(): SilenceRegion {
  return { startSeconds: 0, endSeconds: 0, avgDb: 0 };
}

export const SilenceRegion: MessageFns<SilenceRegion> = {
  encode(message: SilenceRegion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.startSeconds !== 0) {
      writer.uint32(9).double(message.startSeconds);
    }
    if (message.endSeconds !== 0) {
      writer.uint32(17).double(message.endSeconds);
    }
    if (message.avgDb !== 0) {
      writer.uint32(25).double(message.avgDb);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SilenceRegion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSilenceRegion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.startSeconds = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.endSeconds = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.avgDb = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SilenceRegion {
    return {
      startSeconds: isSet(object.startSeconds)
        ? globalThis.Number(object.startSeconds)
        : isSet(object.start_seconds)
        ? globalThis.Number(object.start_seconds)
        : 0,
      endSeconds: isSet(object.endSeconds)
        ? globalThis.Number(object.endSeconds)
        : isSet(object.end_seconds)
        ? globalThis.Number(object.end_seconds)
        : 0,
      avgDb: isSet(object.avgDb)
        ? globalThis.Number(object.avgDb)
        : isSet(object.avg_db)
        ? globalThis.Number(object.avg_db)
        : 0,
    };
  },

  toJSON(message: SilenceRegion): unknown {
    const obj: any = {};
    if (message.startSeconds !== 0) {
      obj.startSeconds = message.startSeconds;
    }
    if (message.endSeconds !== 0) {
      obj.endSeconds = message.endSeconds;
    }
    if (message.avgDb !== 0) {
      obj.avgDb = message.avgDb;
    }
    return obj;
  },

  create(base?: DeepPartial<SilenceRegion>): SilenceRegion {
    return SilenceRegion.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SilenceRegion>): SilenceRegion {
    const message = createBaseSilenceRegion();
    message.startSeconds = object.startSeconds ?? 0;
    message.endSeconds = object.endSeconds ?? 0;
    message.avgDb = object.avgDb ?? 0;
    return message;
  },
};

function createBaseDetectScenesRequest(): DetectScenesRequest {
  return { filePath: "", threshold: 0 };
}

export const DetectScenesRequest: MessageFns<DetectScenesRequest> = {
  encode(message: DetectScenesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filePath !== "") {
      writer.uint32(10).string(message.filePath);
    }
    if (message.threshold !== 0) {
      writer.uint32(17).double(message.threshold);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectScenesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectScenesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.threshold = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectScenesRequest {
    return {
      filePath: isSet(object.filePath)
        ? globalThis.String(object.filePath)
        : isSet(object.file_path)
        ? globalThis.String(object.file_path)
        : "",
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
    };
  },

  toJSON(message: DetectScenesRequest): unknown {
    const obj: any = {};
    if (message.filePath !== "") {
      obj.filePath = message.filePath;
    }
    if (message.threshold !== 0) {
      obj.threshold = message.threshold;
    }
    return obj;
  },

  create(base?: DeepPartial<DetectScenesRequest>): DetectScenesRequest {
    return DetectScenesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DetectScenesRequest>): DetectScenesRequest {
    const message = createBaseDetectScenesRequest();
    message.filePath = object.filePath ?? "";
    message.threshold = object.threshold ?? 0;
    return message;
  },
};

function createBaseDetectScenesResponse(): DetectScenesResponse {
  return { scenes: [] };
}

export const DetectScenesResponse: MessageFns<DetectScenesResponse> = {
  encode(message: DetectScenesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.scenes) {
      SceneChange.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectScenesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectScenesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scenes.push(SceneChange.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectScenesResponse {
    return {
      scenes: globalThis.Array.isArray(object?.scenes) ? object.scenes.map((e: any) => SceneChange.fromJSON(e)) : [],
    };
  },

  toJSON(message: DetectScenesResponse): unknown {
    const obj: any = {};
    if (message.scenes?.length) {
      obj.scenes = message.scenes.map((e) => SceneChange.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<DetectScenesResponse>): DetectScenesResponse {
    return DetectScenesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DetectScenesResponse>): DetectScenesResponse {
    const message = createBaseDetectScenesResponse();
    message.scenes = object.scenes?.map((e) => SceneChange.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSceneChange(): SceneChange {
  return { timecode: undefined, confidence: 0 };
}

export const SceneChange: MessageFns<SceneChange> = {
  encode(message: SceneChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timecode !== undefined) {
      Timecode.encode(message.timecode, writer.uint32(10).fork()).join();
    }
    if (message.confidence !== 0) {
      writer.uint32(17).double(message.confidence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SceneChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSceneChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timecode = Timecode.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.confidence = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SceneChange {
    return {
      timecode: isSet(object.timecode) ? Timecode.fromJSON(object.timecode) : undefined,
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
    };
  },

  toJSON(message: SceneChange): unknown {
    const obj: any = {};
    if (message.timecode !== undefined) {
      obj.timecode = Timecode.toJSON(message.timecode);
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    return obj;
  },

  create(base?: DeepPartial<SceneChange>): SceneChange {
    return SceneChange.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SceneChange>): SceneChange {
    const message = createBaseSceneChange();
    message.timecode = (object.timecode !== undefined && object.timecode !== null)
      ? Timecode.fromPartial(object.timecode)
      : undefined;
    message.confidence = object.confidence ?? 0;
    return message;
  },
};

/**
 * MediaEngine handles media file scanning, probing, and analysis.
 * Implemented by the Rust engine.
 */
export type MediaEngineServiceDefinition = typeof MediaEngineServiceDefinition;
export const MediaEngineServiceDefinition = {
  name: "MediaEngineService",
  fullName: "premierpro.media.v1.MediaEngineService",
  methods: {
    /** Scan a directory and index all media assets. */
    scanAssets: {
      name: "ScanAssets",
      requestType: ScanAssetsRequest,
      requestStream: false,
      responseType: ScanAssetsResponse,
      responseStream: false,
      options: {},
    },
    /** Probe a single media file for detailed metadata. */
    probeMedia: {
      name: "ProbeMedia",
      requestType: ProbeMediaRequest,
      requestStream: false,
      responseType: ProbeMediaResponse,
      responseStream: false,
      options: {},
    },
    /** Generate a thumbnail at a specific timestamp. */
    generateThumbnail: {
      name: "GenerateThumbnail",
      requestType: GenerateThumbnailRequest,
      requestStream: false,
      responseType: GenerateThumbnailResponse,
      responseStream: false,
      options: {},
    },
    /** Analyze audio waveform and detect silence regions. */
    analyzeWaveform: {
      name: "AnalyzeWaveform",
      requestType: AnalyzeWaveformRequest,
      requestStream: false,
      responseType: AnalyzeWaveformResponse,
      responseStream: false,
      options: {},
    },
    /** Detect scene changes in a video file. */
    detectScenes: {
      name: "DetectScenes",
      requestType: DetectScenesRequest,
      requestStream: false,
      responseType: DetectScenesResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface MediaEngineServiceImplementation<CallContextExt = {}> {
  /** Scan a directory and index all media assets. */
  scanAssets(
    request: ScanAssetsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ScanAssetsResponse>>;
  /** Probe a single media file for detailed metadata. */
  probeMedia(
    request: ProbeMediaRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ProbeMediaResponse>>;
  /** Generate a thumbnail at a specific timestamp. */
  generateThumbnail(
    request: GenerateThumbnailRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GenerateThumbnailResponse>>;
  /** Analyze audio waveform and detect silence regions. */
  analyzeWaveform(
    request: AnalyzeWaveformRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<AnalyzeWaveformResponse>>;
  /** Detect scene changes in a video file. */
  detectScenes(
    request: DetectScenesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<DetectScenesResponse>>;
}

export interface MediaEngineServiceClient<CallOptionsExt = {}> {
  /** Scan a directory and index all media assets. */
  scanAssets(
    request: DeepPartial<ScanAssetsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ScanAssetsResponse>;
  /** Probe a single media file for detailed metadata. */
  probeMedia(
    request: DeepPartial<ProbeMediaRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ProbeMediaResponse>;
  /** Generate a thumbnail at a specific timestamp. */
  generateThumbnail(
    request: DeepPartial<GenerateThumbnailRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GenerateThumbnailResponse>;
  /** Analyze audio waveform and detect silence regions. */
  analyzeWaveform(
    request: DeepPartial<AnalyzeWaveformRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<AnalyzeWaveformResponse>;
  /** Detect scene changes in a video file. */
  detectScenes(
    request: DeepPartial<DetectScenesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<DetectScenesResponse>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
