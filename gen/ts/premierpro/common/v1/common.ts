// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: premierpro/common/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "premierpro.common.v1";

/** Type of media asset. */
export enum AssetType {
  ASSET_TYPE_UNSPECIFIED = 0,
  ASSET_TYPE_VIDEO = 1,
  ASSET_TYPE_AUDIO = 2,
  ASSET_TYPE_IMAGE = 3,
  ASSET_TYPE_GRAPHICS = 4,
  UNRECOGNIZED = -1,
}

export function assetTypeFromJSON(object: any): AssetType {
  switch (object) {
    case 0:
    case "ASSET_TYPE_UNSPECIFIED":
      return AssetType.ASSET_TYPE_UNSPECIFIED;
    case 1:
    case "ASSET_TYPE_VIDEO":
      return AssetType.ASSET_TYPE_VIDEO;
    case 2:
    case "ASSET_TYPE_AUDIO":
      return AssetType.ASSET_TYPE_AUDIO;
    case 3:
    case "ASSET_TYPE_IMAGE":
      return AssetType.ASSET_TYPE_IMAGE;
    case 4:
    case "ASSET_TYPE_GRAPHICS":
      return AssetType.ASSET_TYPE_GRAPHICS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AssetType.UNRECOGNIZED;
  }
}

export function assetTypeToJSON(object: AssetType): string {
  switch (object) {
    case AssetType.ASSET_TYPE_UNSPECIFIED:
      return "ASSET_TYPE_UNSPECIFIED";
    case AssetType.ASSET_TYPE_VIDEO:
      return "ASSET_TYPE_VIDEO";
    case AssetType.ASSET_TYPE_AUDIO:
      return "ASSET_TYPE_AUDIO";
    case AssetType.ASSET_TYPE_IMAGE:
      return "ASSET_TYPE_IMAGE";
    case AssetType.ASSET_TYPE_GRAPHICS:
      return "ASSET_TYPE_GRAPHICS";
    case AssetType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Track type. */
export enum TrackType {
  TRACK_TYPE_UNSPECIFIED = 0,
  TRACK_TYPE_VIDEO = 1,
  TRACK_TYPE_AUDIO = 2,
  UNRECOGNIZED = -1,
}

export function trackTypeFromJSON(object: any): TrackType {
  switch (object) {
    case 0:
    case "TRACK_TYPE_UNSPECIFIED":
      return TrackType.TRACK_TYPE_UNSPECIFIED;
    case 1:
    case "TRACK_TYPE_VIDEO":
      return TrackType.TRACK_TYPE_VIDEO;
    case 2:
    case "TRACK_TYPE_AUDIO":
      return TrackType.TRACK_TYPE_AUDIO;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TrackType.UNRECOGNIZED;
  }
}

export function trackTypeToJSON(object: TrackType): string {
  switch (object) {
    case TrackType.TRACK_TYPE_UNSPECIFIED:
      return "TRACK_TYPE_UNSPECIFIED";
    case TrackType.TRACK_TYPE_VIDEO:
      return "TRACK_TYPE_VIDEO";
    case TrackType.TRACK_TYPE_AUDIO:
      return "TRACK_TYPE_AUDIO";
    case TrackType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Status for async operations. */
export enum OperationStatus {
  OPERATION_STATUS_UNSPECIFIED = 0,
  OPERATION_STATUS_PENDING = 1,
  OPERATION_STATUS_RUNNING = 2,
  OPERATION_STATUS_COMPLETED = 3,
  OPERATION_STATUS_FAILED = 4,
  UNRECOGNIZED = -1,
}

export function operationStatusFromJSON(object: any): OperationStatus {
  switch (object) {
    case 0:
    case "OPERATION_STATUS_UNSPECIFIED":
      return OperationStatus.OPERATION_STATUS_UNSPECIFIED;
    case 1:
    case "OPERATION_STATUS_PENDING":
      return OperationStatus.OPERATION_STATUS_PENDING;
    case 2:
    case "OPERATION_STATUS_RUNNING":
      return OperationStatus.OPERATION_STATUS_RUNNING;
    case 3:
    case "OPERATION_STATUS_COMPLETED":
      return OperationStatus.OPERATION_STATUS_COMPLETED;
    case 4:
    case "OPERATION_STATUS_FAILED":
      return OperationStatus.OPERATION_STATUS_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OperationStatus.UNRECOGNIZED;
  }
}

export function operationStatusToJSON(object: OperationStatus): string {
  switch (object) {
    case OperationStatus.OPERATION_STATUS_UNSPECIFIED:
      return "OPERATION_STATUS_UNSPECIFIED";
    case OperationStatus.OPERATION_STATUS_PENDING:
      return "OPERATION_STATUS_PENDING";
    case OperationStatus.OPERATION_STATUS_RUNNING:
      return "OPERATION_STATUS_RUNNING";
    case OperationStatus.OPERATION_STATUS_COMPLETED:
      return "OPERATION_STATUS_COMPLETED";
    case OperationStatus.OPERATION_STATUS_FAILED:
      return "OPERATION_STATUS_FAILED";
    case OperationStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Timecode in HH:MM:SS:FF format or as total frames. */
export interface Timecode {
  hours: number;
  minutes: number;
  seconds: number;
  frames: number;
  frameRate: number;
}

/** A time range defined by in and out points. */
export interface TimeRange {
  inPoint: Timecode | undefined;
  outPoint: Timecode | undefined;
}

/** Video resolution. */
export interface Resolution {
  width: number;
  height: number;
}

/** Video stream metadata. */
export interface VideoInfo {
  codec: string;
  resolution: Resolution | undefined;
  frameRate: number;
  bitrateBps: number;
  pixelFormat: string;
  durationSeconds: number;
}

/** Audio stream metadata. */
export interface AudioInfo {
  codec: string;
  sampleRate: number;
  channels: number;
  bitrateBps: number;
  durationSeconds: number;
}

/** A media asset with full metadata. */
export interface Asset {
  id: string;
  filePath: string;
  fileName: string;
  fileSizeBytes: number;
  mimeType: string;
  assetType: AssetType;
  video: VideoInfo | undefined;
  audio: AudioInfo | undefined;
  metadata: { [key: string]: string };
  fingerprint: string;
}

export interface Asset_MetadataEntry {
  key: string;
  value: string;
}

/** A single entry in an Edit Decision List. */
export interface EDLEntry {
  index: number;
  sourceAssetId: string;
  sourceRange: TimeRange | undefined;
  timelineRange: TimeRange | undefined;
  track: TrackTarget | undefined;
  transition: TransitionInfo | undefined;
  effects: EffectInfo[];
  notes: string;
}

/** Target track for clip placement. */
export interface TrackTarget {
  type: TrackType;
  trackIndex: number;
}

/** Transition between clips. */
export interface TransitionInfo {
  type: string;
  durationSeconds: number;
  alignment: string;
}

/** Effect applied to a clip. */
export interface EffectInfo {
  name: string;
  parameters: { [key: string]: string };
}

export interface EffectInfo_ParametersEntry {
  key: string;
  value: string;
}

/** A full Edit Decision List. */
export interface EditDecisionList {
  id: string;
  name: string;
  sequenceResolution: Resolution | undefined;
  sequenceFrameRate: number;
  entries: EDLEntry[];
}

/** Text overlay style. */
export interface TextStyle {
  fontFamily: string;
  fontSize: number;
  colorHex: string;
  alignment: string;
  backgroundColorHex: string;
  backgroundOpacity: number;
  position: Position | undefined;
}

/** Position on screen (normalized 0.0-1.0). */
export interface Position {
  x: number;
  y: number;
}

function createBaseTimecode(): Timecode {
  return { hours: 0, minutes: 0, seconds: 0, frames: 0, frameRate: 0 };
}

export const Timecode: MessageFns<Timecode> = {
  encode(message: Timecode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hours !== 0) {
      writer.uint32(8).uint32(message.hours);
    }
    if (message.minutes !== 0) {
      writer.uint32(16).uint32(message.minutes);
    }
    if (message.seconds !== 0) {
      writer.uint32(24).uint32(message.seconds);
    }
    if (message.frames !== 0) {
      writer.uint32(32).uint32(message.frames);
    }
    if (message.frameRate !== 0) {
      writer.uint32(41).double(message.frameRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Timecode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimecode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hours = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minutes = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.seconds = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.frames = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.frameRate = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Timecode {
    return {
      hours: isSet(object.hours) ? globalThis.Number(object.hours) : 0,
      minutes: isSet(object.minutes) ? globalThis.Number(object.minutes) : 0,
      seconds: isSet(object.seconds) ? globalThis.Number(object.seconds) : 0,
      frames: isSet(object.frames) ? globalThis.Number(object.frames) : 0,
      frameRate: isSet(object.frameRate)
        ? globalThis.Number(object.frameRate)
        : isSet(object.frame_rate)
        ? globalThis.Number(object.frame_rate)
        : 0,
    };
  },

  toJSON(message: Timecode): unknown {
    const obj: any = {};
    if (message.hours !== 0) {
      obj.hours = Math.round(message.hours);
    }
    if (message.minutes !== 0) {
      obj.minutes = Math.round(message.minutes);
    }
    if (message.seconds !== 0) {
      obj.seconds = Math.round(message.seconds);
    }
    if (message.frames !== 0) {
      obj.frames = Math.round(message.frames);
    }
    if (message.frameRate !== 0) {
      obj.frameRate = message.frameRate;
    }
    return obj;
  },

  create(base?: DeepPartial<Timecode>): Timecode {
    return Timecode.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Timecode>): Timecode {
    const message = createBaseTimecode();
    message.hours = object.hours ?? 0;
    message.minutes = object.minutes ?? 0;
    message.seconds = object.seconds ?? 0;
    message.frames = object.frames ?? 0;
    message.frameRate = object.frameRate ?? 0;
    return message;
  },
};

function createBaseTimeRange(): TimeRange {
  return { inPoint: undefined, outPoint: undefined };
}

export const TimeRange: MessageFns<TimeRange> = {
  encode(message: TimeRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inPoint !== undefined) {
      Timecode.encode(message.inPoint, writer.uint32(10).fork()).join();
    }
    if (message.outPoint !== undefined) {
      Timecode.encode(message.outPoint, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimeRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.inPoint = Timecode.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outPoint = Timecode.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeRange {
    return {
      inPoint: isSet(object.inPoint)
        ? Timecode.fromJSON(object.inPoint)
        : isSet(object.in_point)
        ? Timecode.fromJSON(object.in_point)
        : undefined,
      outPoint: isSet(object.outPoint)
        ? Timecode.fromJSON(object.outPoint)
        : isSet(object.out_point)
        ? Timecode.fromJSON(object.out_point)
        : undefined,
    };
  },

  toJSON(message: TimeRange): unknown {
    const obj: any = {};
    if (message.inPoint !== undefined) {
      obj.inPoint = Timecode.toJSON(message.inPoint);
    }
    if (message.outPoint !== undefined) {
      obj.outPoint = Timecode.toJSON(message.outPoint);
    }
    return obj;
  },

  create(base?: DeepPartial<TimeRange>): TimeRange {
    return TimeRange.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TimeRange>): TimeRange {
    const message = createBaseTimeRange();
    message.inPoint = (object.inPoint !== undefined && object.inPoint !== null)
      ? Timecode.fromPartial(object.inPoint)
      : undefined;
    message.outPoint = (object.outPoint !== undefined && object.outPoint !== null)
      ? Timecode.fromPartial(object.outPoint)
      : undefined;
    return message;
  },
};

function createBaseResolution(): Resolution {
  return { width: 0, height: 0 };
}

export const Resolution: MessageFns<Resolution> = {
  encode(message: Resolution, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.width !== 0) {
      writer.uint32(8).uint32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(16).uint32(message.height);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Resolution {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResolution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.width = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.height = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Resolution {
    return {
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
    };
  },

  toJSON(message: Resolution): unknown {
    const obj: any = {};
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    return obj;
  },

  create(base?: DeepPartial<Resolution>): Resolution {
    return Resolution.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Resolution>): Resolution {
    const message = createBaseResolution();
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    return message;
  },
};

function createBaseVideoInfo(): VideoInfo {
  return { codec: "", resolution: undefined, frameRate: 0, bitrateBps: 0, pixelFormat: "", durationSeconds: 0 };
}

export const VideoInfo: MessageFns<VideoInfo> = {
  encode(message: VideoInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.codec !== "") {
      writer.uint32(10).string(message.codec);
    }
    if (message.resolution !== undefined) {
      Resolution.encode(message.resolution, writer.uint32(18).fork()).join();
    }
    if (message.frameRate !== 0) {
      writer.uint32(25).double(message.frameRate);
    }
    if (message.bitrateBps !== 0) {
      writer.uint32(32).uint64(message.bitrateBps);
    }
    if (message.pixelFormat !== "") {
      writer.uint32(42).string(message.pixelFormat);
    }
    if (message.durationSeconds !== 0) {
      writer.uint32(49).double(message.durationSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.codec = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.resolution = Resolution.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.frameRate = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.bitrateBps = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pixelFormat = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.durationSeconds = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoInfo {
    return {
      codec: isSet(object.codec) ? globalThis.String(object.codec) : "",
      resolution: isSet(object.resolution) ? Resolution.fromJSON(object.resolution) : undefined,
      frameRate: isSet(object.frameRate)
        ? globalThis.Number(object.frameRate)
        : isSet(object.frame_rate)
        ? globalThis.Number(object.frame_rate)
        : 0,
      bitrateBps: isSet(object.bitrateBps)
        ? globalThis.Number(object.bitrateBps)
        : isSet(object.bitrate_bps)
        ? globalThis.Number(object.bitrate_bps)
        : 0,
      pixelFormat: isSet(object.pixelFormat)
        ? globalThis.String(object.pixelFormat)
        : isSet(object.pixel_format)
        ? globalThis.String(object.pixel_format)
        : "",
      durationSeconds: isSet(object.durationSeconds)
        ? globalThis.Number(object.durationSeconds)
        : isSet(object.duration_seconds)
        ? globalThis.Number(object.duration_seconds)
        : 0,
    };
  },

  toJSON(message: VideoInfo): unknown {
    const obj: any = {};
    if (message.codec !== "") {
      obj.codec = message.codec;
    }
    if (message.resolution !== undefined) {
      obj.resolution = Resolution.toJSON(message.resolution);
    }
    if (message.frameRate !== 0) {
      obj.frameRate = message.frameRate;
    }
    if (message.bitrateBps !== 0) {
      obj.bitrateBps = Math.round(message.bitrateBps);
    }
    if (message.pixelFormat !== "") {
      obj.pixelFormat = message.pixelFormat;
    }
    if (message.durationSeconds !== 0) {
      obj.durationSeconds = message.durationSeconds;
    }
    return obj;
  },

  create(base?: DeepPartial<VideoInfo>): VideoInfo {
    return VideoInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VideoInfo>): VideoInfo {
    const message = createBaseVideoInfo();
    message.codec = object.codec ?? "";
    message.resolution = (object.resolution !== undefined && object.resolution !== null)
      ? Resolution.fromPartial(object.resolution)
      : undefined;
    message.frameRate = object.frameRate ?? 0;
    message.bitrateBps = object.bitrateBps ?? 0;
    message.pixelFormat = object.pixelFormat ?? "";
    message.durationSeconds = object.durationSeconds ?? 0;
    return message;
  },
};

function createBaseAudioInfo(): AudioInfo {
  return { codec: "", sampleRate: 0, channels: 0, bitrateBps: 0, durationSeconds: 0 };
}

export const AudioInfo: MessageFns<AudioInfo> = {
  encode(message: AudioInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.codec !== "") {
      writer.uint32(10).string(message.codec);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(16).uint32(message.sampleRate);
    }
    if (message.channels !== 0) {
      writer.uint32(24).uint32(message.channels);
    }
    if (message.bitrateBps !== 0) {
      writer.uint32(32).uint64(message.bitrateBps);
    }
    if (message.durationSeconds !== 0) {
      writer.uint32(41).double(message.durationSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AudioInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAudioInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.codec = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sampleRate = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.channels = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.bitrateBps = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.durationSeconds = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AudioInfo {
    return {
      codec: isSet(object.codec) ? globalThis.String(object.codec) : "",
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
      channels: isSet(object.channels) ? globalThis.Number(object.channels) : 0,
      bitrateBps: isSet(object.bitrateBps)
        ? globalThis.Number(object.bitrateBps)
        : isSet(object.bitrate_bps)
        ? globalThis.Number(object.bitrate_bps)
        : 0,
      durationSeconds: isSet(object.durationSeconds)
        ? globalThis.Number(object.durationSeconds)
        : isSet(object.duration_seconds)
        ? globalThis.Number(object.duration_seconds)
        : 0,
    };
  },

  toJSON(message: AudioInfo): unknown {
    const obj: any = {};
    if (message.codec !== "") {
      obj.codec = message.codec;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    if (message.channels !== 0) {
      obj.channels = Math.round(message.channels);
    }
    if (message.bitrateBps !== 0) {
      obj.bitrateBps = Math.round(message.bitrateBps);
    }
    if (message.durationSeconds !== 0) {
      obj.durationSeconds = message.durationSeconds;
    }
    return obj;
  },

  create(base?: DeepPartial<AudioInfo>): AudioInfo {
    return AudioInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AudioInfo>): AudioInfo {
    const message = createBaseAudioInfo();
    message.codec = object.codec ?? "";
    message.sampleRate = object.sampleRate ?? 0;
    message.channels = object.channels ?? 0;
    message.bitrateBps = object.bitrateBps ?? 0;
    message.durationSeconds = object.durationSeconds ?? 0;
    return message;
  },
};

function createBaseAsset(): Asset {
  return {
    id: "",
    filePath: "",
    fileName: "",
    fileSizeBytes: 0,
    mimeType: "",
    assetType: 0,
    video: undefined,
    audio: undefined,
    metadata: {},
    fingerprint: "",
  };
}

export const Asset: MessageFns<Asset> = {
  encode(message: Asset, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.filePath !== "") {
      writer.uint32(18).string(message.filePath);
    }
    if (message.fileName !== "") {
      writer.uint32(26).string(message.fileName);
    }
    if (message.fileSizeBytes !== 0) {
      writer.uint32(32).uint64(message.fileSizeBytes);
    }
    if (message.mimeType !== "") {
      writer.uint32(42).string(message.mimeType);
    }
    if (message.assetType !== 0) {
      writer.uint32(48).int32(message.assetType);
    }
    if (message.video !== undefined) {
      VideoInfo.encode(message.video, writer.uint32(58).fork()).join();
    }
    if (message.audio !== undefined) {
      AudioInfo.encode(message.audio, writer.uint32(66).fork()).join();
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      Asset_MetadataEntry.encode({ key: key as any, value }, writer.uint32(74).fork()).join();
    });
    if (message.fingerprint !== "") {
      writer.uint32(82).string(message.fingerprint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Asset {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAsset();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fileSizeBytes = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.assetType = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.video = VideoInfo.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.audio = AudioInfo.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          const entry9 = Asset_MetadataEntry.decode(reader, reader.uint32());
          if (entry9.value !== undefined) {
            message.metadata[entry9.key] = entry9.value;
          }
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.fingerprint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Asset {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      filePath: isSet(object.filePath)
        ? globalThis.String(object.filePath)
        : isSet(object.file_path)
        ? globalThis.String(object.file_path)
        : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      fileSizeBytes: isSet(object.fileSizeBytes)
        ? globalThis.Number(object.fileSizeBytes)
        : isSet(object.file_size_bytes)
        ? globalThis.Number(object.file_size_bytes)
        : 0,
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      assetType: isSet(object.assetType)
        ? assetTypeFromJSON(object.assetType)
        : isSet(object.asset_type)
        ? assetTypeFromJSON(object.asset_type)
        : 0,
      video: isSet(object.video) ? VideoInfo.fromJSON(object.video) : undefined,
      audio: isSet(object.audio) ? AudioInfo.fromJSON(object.audio) : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      fingerprint: isSet(object.fingerprint) ? globalThis.String(object.fingerprint) : "",
    };
  },

  toJSON(message: Asset): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.filePath !== "") {
      obj.filePath = message.filePath;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.fileSizeBytes !== 0) {
      obj.fileSizeBytes = Math.round(message.fileSizeBytes);
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.assetType !== 0) {
      obj.assetType = assetTypeToJSON(message.assetType);
    }
    if (message.video !== undefined) {
      obj.video = VideoInfo.toJSON(message.video);
    }
    if (message.audio !== undefined) {
      obj.audio = AudioInfo.toJSON(message.audio);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.fingerprint !== "") {
      obj.fingerprint = message.fingerprint;
    }
    return obj;
  },

  create(base?: DeepPartial<Asset>): Asset {
    return Asset.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Asset>): Asset {
    const message = createBaseAsset();
    message.id = object.id ?? "";
    message.filePath = object.filePath ?? "";
    message.fileName = object.fileName ?? "";
    message.fileSizeBytes = object.fileSizeBytes ?? 0;
    message.mimeType = object.mimeType ?? "";
    message.assetType = object.assetType ?? 0;
    message.video = (object.video !== undefined && object.video !== null)
      ? VideoInfo.fromPartial(object.video)
      : undefined;
    message.audio = (object.audio !== undefined && object.audio !== null)
      ? AudioInfo.fromPartial(object.audio)
      : undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.fingerprint = object.fingerprint ?? "";
    return message;
  },
};

function createBaseAsset_MetadataEntry(): Asset_MetadataEntry {
  return { key: "", value: "" };
}

export const Asset_MetadataEntry: MessageFns<Asset_MetadataEntry> = {
  encode(message: Asset_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Asset_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAsset_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Asset_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Asset_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<Asset_MetadataEntry>): Asset_MetadataEntry {
    return Asset_MetadataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Asset_MetadataEntry>): Asset_MetadataEntry {
    const message = createBaseAsset_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseEDLEntry(): EDLEntry {
  return {
    index: 0,
    sourceAssetId: "",
    sourceRange: undefined,
    timelineRange: undefined,
    track: undefined,
    transition: undefined,
    effects: [],
    notes: "",
  };
}

export const EDLEntry: MessageFns<EDLEntry> = {
  encode(message: EDLEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.sourceAssetId !== "") {
      writer.uint32(18).string(message.sourceAssetId);
    }
    if (message.sourceRange !== undefined) {
      TimeRange.encode(message.sourceRange, writer.uint32(26).fork()).join();
    }
    if (message.timelineRange !== undefined) {
      TimeRange.encode(message.timelineRange, writer.uint32(34).fork()).join();
    }
    if (message.track !== undefined) {
      TrackTarget.encode(message.track, writer.uint32(42).fork()).join();
    }
    if (message.transition !== undefined) {
      TransitionInfo.encode(message.transition, writer.uint32(50).fork()).join();
    }
    for (const v of message.effects) {
      EffectInfo.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.notes !== "") {
      writer.uint32(66).string(message.notes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EDLEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEDLEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourceAssetId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sourceRange = TimeRange.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timelineRange = TimeRange.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.track = TrackTarget.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.transition = TransitionInfo.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.effects.push(EffectInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EDLEntry {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      sourceAssetId: isSet(object.sourceAssetId)
        ? globalThis.String(object.sourceAssetId)
        : isSet(object.source_asset_id)
        ? globalThis.String(object.source_asset_id)
        : "",
      sourceRange: isSet(object.sourceRange)
        ? TimeRange.fromJSON(object.sourceRange)
        : isSet(object.source_range)
        ? TimeRange.fromJSON(object.source_range)
        : undefined,
      timelineRange: isSet(object.timelineRange)
        ? TimeRange.fromJSON(object.timelineRange)
        : isSet(object.timeline_range)
        ? TimeRange.fromJSON(object.timeline_range)
        : undefined,
      track: isSet(object.track) ? TrackTarget.fromJSON(object.track) : undefined,
      transition: isSet(object.transition) ? TransitionInfo.fromJSON(object.transition) : undefined,
      effects: globalThis.Array.isArray(object?.effects) ? object.effects.map((e: any) => EffectInfo.fromJSON(e)) : [],
      notes: isSet(object.notes) ? globalThis.String(object.notes) : "",
    };
  },

  toJSON(message: EDLEntry): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.sourceAssetId !== "") {
      obj.sourceAssetId = message.sourceAssetId;
    }
    if (message.sourceRange !== undefined) {
      obj.sourceRange = TimeRange.toJSON(message.sourceRange);
    }
    if (message.timelineRange !== undefined) {
      obj.timelineRange = TimeRange.toJSON(message.timelineRange);
    }
    if (message.track !== undefined) {
      obj.track = TrackTarget.toJSON(message.track);
    }
    if (message.transition !== undefined) {
      obj.transition = TransitionInfo.toJSON(message.transition);
    }
    if (message.effects?.length) {
      obj.effects = message.effects.map((e) => EffectInfo.toJSON(e));
    }
    if (message.notes !== "") {
      obj.notes = message.notes;
    }
    return obj;
  },

  create(base?: DeepPartial<EDLEntry>): EDLEntry {
    return EDLEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EDLEntry>): EDLEntry {
    const message = createBaseEDLEntry();
    message.index = object.index ?? 0;
    message.sourceAssetId = object.sourceAssetId ?? "";
    message.sourceRange = (object.sourceRange !== undefined && object.sourceRange !== null)
      ? TimeRange.fromPartial(object.sourceRange)
      : undefined;
    message.timelineRange = (object.timelineRange !== undefined && object.timelineRange !== null)
      ? TimeRange.fromPartial(object.timelineRange)
      : undefined;
    message.track = (object.track !== undefined && object.track !== null)
      ? TrackTarget.fromPartial(object.track)
      : undefined;
    message.transition = (object.transition !== undefined && object.transition !== null)
      ? TransitionInfo.fromPartial(object.transition)
      : undefined;
    message.effects = object.effects?.map((e) => EffectInfo.fromPartial(e)) || [];
    message.notes = object.notes ?? "";
    return message;
  },
};

function createBaseTrackTarget(): TrackTarget {
  return { type: 0, trackIndex: 0 };
}

export const TrackTarget: MessageFns<TrackTarget> = {
  encode(message: TrackTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.trackIndex !== 0) {
      writer.uint32(16).uint32(message.trackIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.trackIndex = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackTarget {
    return {
      type: isSet(object.type) ? trackTypeFromJSON(object.type) : 0,
      trackIndex: isSet(object.trackIndex)
        ? globalThis.Number(object.trackIndex)
        : isSet(object.track_index)
        ? globalThis.Number(object.track_index)
        : 0,
    };
  },

  toJSON(message: TrackTarget): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = trackTypeToJSON(message.type);
    }
    if (message.trackIndex !== 0) {
      obj.trackIndex = Math.round(message.trackIndex);
    }
    return obj;
  },

  create(base?: DeepPartial<TrackTarget>): TrackTarget {
    return TrackTarget.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TrackTarget>): TrackTarget {
    const message = createBaseTrackTarget();
    message.type = object.type ?? 0;
    message.trackIndex = object.trackIndex ?? 0;
    return message;
  },
};

function createBaseTransitionInfo(): TransitionInfo {
  return { type: "", durationSeconds: 0, alignment: "" };
}

export const TransitionInfo: MessageFns<TransitionInfo> = {
  encode(message: TransitionInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.durationSeconds !== 0) {
      writer.uint32(17).double(message.durationSeconds);
    }
    if (message.alignment !== "") {
      writer.uint32(26).string(message.alignment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransitionInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransitionInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.durationSeconds = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.alignment = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransitionInfo {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      durationSeconds: isSet(object.durationSeconds)
        ? globalThis.Number(object.durationSeconds)
        : isSet(object.duration_seconds)
        ? globalThis.Number(object.duration_seconds)
        : 0,
      alignment: isSet(object.alignment) ? globalThis.String(object.alignment) : "",
    };
  },

  toJSON(message: TransitionInfo): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.durationSeconds !== 0) {
      obj.durationSeconds = message.durationSeconds;
    }
    if (message.alignment !== "") {
      obj.alignment = message.alignment;
    }
    return obj;
  },

  create(base?: DeepPartial<TransitionInfo>): TransitionInfo {
    return TransitionInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TransitionInfo>): TransitionInfo {
    const message = createBaseTransitionInfo();
    message.type = object.type ?? "";
    message.durationSeconds = object.durationSeconds ?? 0;
    message.alignment = object.alignment ?? "";
    return message;
  },
};

function createBaseEffectInfo(): EffectInfo {
  return { name: "", parameters: {} };
}

export const EffectInfo: MessageFns<EffectInfo> = {
  encode(message: EffectInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    globalThis.Object.entries(message.parameters).forEach(([key, value]: [string, string]) => {
      EffectInfo_ParametersEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EffectInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEffectInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = EffectInfo_ParametersEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.parameters[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EffectInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      parameters: isObject(object.parameters)
        ? (globalThis.Object.entries(object.parameters) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: EffectInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.parameters) {
      const entries = globalThis.Object.entries(message.parameters) as [string, string][];
      if (entries.length > 0) {
        obj.parameters = {};
        entries.forEach(([k, v]) => {
          obj.parameters[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<EffectInfo>): EffectInfo {
    return EffectInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EffectInfo>): EffectInfo {
    const message = createBaseEffectInfo();
    message.name = object.name ?? "";
    message.parameters = (globalThis.Object.entries(object.parameters ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseEffectInfo_ParametersEntry(): EffectInfo_ParametersEntry {
  return { key: "", value: "" };
}

export const EffectInfo_ParametersEntry: MessageFns<EffectInfo_ParametersEntry> = {
  encode(message: EffectInfo_ParametersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EffectInfo_ParametersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEffectInfo_ParametersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EffectInfo_ParametersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: EffectInfo_ParametersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<EffectInfo_ParametersEntry>): EffectInfo_ParametersEntry {
    return EffectInfo_ParametersEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EffectInfo_ParametersEntry>): EffectInfo_ParametersEntry {
    const message = createBaseEffectInfo_ParametersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseEditDecisionList(): EditDecisionList {
  return { id: "", name: "", sequenceResolution: undefined, sequenceFrameRate: 0, entries: [] };
}

export const EditDecisionList: MessageFns<EditDecisionList> = {
  encode(message: EditDecisionList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.sequenceResolution !== undefined) {
      Resolution.encode(message.sequenceResolution, writer.uint32(26).fork()).join();
    }
    if (message.sequenceFrameRate !== 0) {
      writer.uint32(33).double(message.sequenceFrameRate);
    }
    for (const v of message.entries) {
      EDLEntry.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EditDecisionList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEditDecisionList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sequenceResolution = Resolution.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.sequenceFrameRate = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.entries.push(EDLEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EditDecisionList {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      sequenceResolution: isSet(object.sequenceResolution)
        ? Resolution.fromJSON(object.sequenceResolution)
        : isSet(object.sequence_resolution)
        ? Resolution.fromJSON(object.sequence_resolution)
        : undefined,
      sequenceFrameRate: isSet(object.sequenceFrameRate)
        ? globalThis.Number(object.sequenceFrameRate)
        : isSet(object.sequence_frame_rate)
        ? globalThis.Number(object.sequence_frame_rate)
        : 0,
      entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e: any) => EDLEntry.fromJSON(e)) : [],
    };
  },

  toJSON(message: EditDecisionList): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.sequenceResolution !== undefined) {
      obj.sequenceResolution = Resolution.toJSON(message.sequenceResolution);
    }
    if (message.sequenceFrameRate !== 0) {
      obj.sequenceFrameRate = message.sequenceFrameRate;
    }
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => EDLEntry.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<EditDecisionList>): EditDecisionList {
    return EditDecisionList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EditDecisionList>): EditDecisionList {
    const message = createBaseEditDecisionList();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.sequenceResolution = (object.sequenceResolution !== undefined && object.sequenceResolution !== null)
      ? Resolution.fromPartial(object.sequenceResolution)
      : undefined;
    message.sequenceFrameRate = object.sequenceFrameRate ?? 0;
    message.entries = object.entries?.map((e) => EDLEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTextStyle(): TextStyle {
  return {
    fontFamily: "",
    fontSize: 0,
    colorHex: "",
    alignment: "",
    backgroundColorHex: "",
    backgroundOpacity: 0,
    position: undefined,
  };
}

export const TextStyle: MessageFns<TextStyle> = {
  encode(message: TextStyle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fontFamily !== "") {
      writer.uint32(10).string(message.fontFamily);
    }
    if (message.fontSize !== 0) {
      writer.uint32(17).double(message.fontSize);
    }
    if (message.colorHex !== "") {
      writer.uint32(26).string(message.colorHex);
    }
    if (message.alignment !== "") {
      writer.uint32(34).string(message.alignment);
    }
    if (message.backgroundColorHex !== "") {
      writer.uint32(42).string(message.backgroundColorHex);
    }
    if (message.backgroundOpacity !== 0) {
      writer.uint32(49).double(message.backgroundOpacity);
    }
    if (message.position !== undefined) {
      Position.encode(message.position, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TextStyle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextStyle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fontFamily = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.fontSize = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.colorHex = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.alignment = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.backgroundColorHex = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.backgroundOpacity = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.position = Position.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextStyle {
    return {
      fontFamily: isSet(object.fontFamily)
        ? globalThis.String(object.fontFamily)
        : isSet(object.font_family)
        ? globalThis.String(object.font_family)
        : "",
      fontSize: isSet(object.fontSize)
        ? globalThis.Number(object.fontSize)
        : isSet(object.font_size)
        ? globalThis.Number(object.font_size)
        : 0,
      colorHex: isSet(object.colorHex)
        ? globalThis.String(object.colorHex)
        : isSet(object.color_hex)
        ? globalThis.String(object.color_hex)
        : "",
      alignment: isSet(object.alignment) ? globalThis.String(object.alignment) : "",
      backgroundColorHex: isSet(object.backgroundColorHex)
        ? globalThis.String(object.backgroundColorHex)
        : isSet(object.background_color_hex)
        ? globalThis.String(object.background_color_hex)
        : "",
      backgroundOpacity: isSet(object.backgroundOpacity)
        ? globalThis.Number(object.backgroundOpacity)
        : isSet(object.background_opacity)
        ? globalThis.Number(object.background_opacity)
        : 0,
      position: isSet(object.position) ? Position.fromJSON(object.position) : undefined,
    };
  },

  toJSON(message: TextStyle): unknown {
    const obj: any = {};
    if (message.fontFamily !== "") {
      obj.fontFamily = message.fontFamily;
    }
    if (message.fontSize !== 0) {
      obj.fontSize = message.fontSize;
    }
    if (message.colorHex !== "") {
      obj.colorHex = message.colorHex;
    }
    if (message.alignment !== "") {
      obj.alignment = message.alignment;
    }
    if (message.backgroundColorHex !== "") {
      obj.backgroundColorHex = message.backgroundColorHex;
    }
    if (message.backgroundOpacity !== 0) {
      obj.backgroundOpacity = message.backgroundOpacity;
    }
    if (message.position !== undefined) {
      obj.position = Position.toJSON(message.position);
    }
    return obj;
  },

  create(base?: DeepPartial<TextStyle>): TextStyle {
    return TextStyle.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TextStyle>): TextStyle {
    const message = createBaseTextStyle();
    message.fontFamily = object.fontFamily ?? "";
    message.fontSize = object.fontSize ?? 0;
    message.colorHex = object.colorHex ?? "";
    message.alignment = object.alignment ?? "";
    message.backgroundColorHex = object.backgroundColorHex ?? "";
    message.backgroundOpacity = object.backgroundOpacity ?? 0;
    message.position = (object.position !== undefined && object.position !== null)
      ? Position.fromPartial(object.position)
      : undefined;
    return message;
  },
};

function createBasePosition(): Position {
  return { x: 0, y: 0 };
}

export const Position: MessageFns<Position> = {
  encode(message: Position, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(9).double(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(17).double(message.y);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Position {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePosition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.x = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.y = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Position {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
    };
  },

  toJSON(message: Position): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    return obj;
  },

  create(base?: DeepPartial<Position>): Position {
    return Position.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Position>): Position {
    const message = createBasePosition();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
