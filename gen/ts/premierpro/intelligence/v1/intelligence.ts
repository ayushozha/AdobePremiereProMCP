// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.4
//   protoc               unknown
// source: premierpro/intelligence/v1/intelligence.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Asset, EditDecisionList, Resolution, TimeRange } from "../../common/v1/common";

export const protobufPackage = "premierpro.intelligence.v1";

export enum SegmentType {
  SEGMENT_TYPE_UNSPECIFIED = 0,
  SEGMENT_TYPE_DIALOGUE = 1,
  SEGMENT_TYPE_ACTION = 2,
  SEGMENT_TYPE_BROLL = 3,
  SEGMENT_TYPE_TRANSITION = 4,
  SEGMENT_TYPE_TITLE = 5,
  SEGMENT_TYPE_LOWER_THIRD = 6,
  SEGMENT_TYPE_VOICEOVER = 7,
  SEGMENT_TYPE_MUSIC = 8,
  SEGMENT_TYPE_SFX = 9,
  UNRECOGNIZED = -1,
}

export function segmentTypeFromJSON(object: any): SegmentType {
  switch (object) {
    case 0:
    case "SEGMENT_TYPE_UNSPECIFIED":
      return SegmentType.SEGMENT_TYPE_UNSPECIFIED;
    case 1:
    case "SEGMENT_TYPE_DIALOGUE":
      return SegmentType.SEGMENT_TYPE_DIALOGUE;
    case 2:
    case "SEGMENT_TYPE_ACTION":
      return SegmentType.SEGMENT_TYPE_ACTION;
    case 3:
    case "SEGMENT_TYPE_BROLL":
      return SegmentType.SEGMENT_TYPE_BROLL;
    case 4:
    case "SEGMENT_TYPE_TRANSITION":
      return SegmentType.SEGMENT_TYPE_TRANSITION;
    case 5:
    case "SEGMENT_TYPE_TITLE":
      return SegmentType.SEGMENT_TYPE_TITLE;
    case 6:
    case "SEGMENT_TYPE_LOWER_THIRD":
      return SegmentType.SEGMENT_TYPE_LOWER_THIRD;
    case 7:
    case "SEGMENT_TYPE_VOICEOVER":
      return SegmentType.SEGMENT_TYPE_VOICEOVER;
    case 8:
    case "SEGMENT_TYPE_MUSIC":
      return SegmentType.SEGMENT_TYPE_MUSIC;
    case 9:
    case "SEGMENT_TYPE_SFX":
      return SegmentType.SEGMENT_TYPE_SFX;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SegmentType.UNRECOGNIZED;
  }
}

export function segmentTypeToJSON(object: SegmentType): string {
  switch (object) {
    case SegmentType.SEGMENT_TYPE_UNSPECIFIED:
      return "SEGMENT_TYPE_UNSPECIFIED";
    case SegmentType.SEGMENT_TYPE_DIALOGUE:
      return "SEGMENT_TYPE_DIALOGUE";
    case SegmentType.SEGMENT_TYPE_ACTION:
      return "SEGMENT_TYPE_ACTION";
    case SegmentType.SEGMENT_TYPE_BROLL:
      return "SEGMENT_TYPE_BROLL";
    case SegmentType.SEGMENT_TYPE_TRANSITION:
      return "SEGMENT_TYPE_TRANSITION";
    case SegmentType.SEGMENT_TYPE_TITLE:
      return "SEGMENT_TYPE_TITLE";
    case SegmentType.SEGMENT_TYPE_LOWER_THIRD:
      return "SEGMENT_TYPE_LOWER_THIRD";
    case SegmentType.SEGMENT_TYPE_VOICEOVER:
      return "SEGMENT_TYPE_VOICEOVER";
    case SegmentType.SEGMENT_TYPE_MUSIC:
      return "SEGMENT_TYPE_MUSIC";
    case SegmentType.SEGMENT_TYPE_SFX:
      return "SEGMENT_TYPE_SFX";
    case SegmentType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum PacingPreset {
  PACING_PRESET_UNSPECIFIED = 0,
  PACING_PRESET_SLOW = 1,
  PACING_PRESET_MODERATE = 2,
  PACING_PRESET_FAST = 3,
  PACING_PRESET_DYNAMIC = 4,
  UNRECOGNIZED = -1,
}

export function pacingPresetFromJSON(object: any): PacingPreset {
  switch (object) {
    case 0:
    case "PACING_PRESET_UNSPECIFIED":
      return PacingPreset.PACING_PRESET_UNSPECIFIED;
    case 1:
    case "PACING_PRESET_SLOW":
      return PacingPreset.PACING_PRESET_SLOW;
    case 2:
    case "PACING_PRESET_MODERATE":
      return PacingPreset.PACING_PRESET_MODERATE;
    case 3:
    case "PACING_PRESET_FAST":
      return PacingPreset.PACING_PRESET_FAST;
    case 4:
    case "PACING_PRESET_DYNAMIC":
      return PacingPreset.PACING_PRESET_DYNAMIC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PacingPreset.UNRECOGNIZED;
  }
}

export function pacingPresetToJSON(object: PacingPreset): string {
  switch (object) {
    case PacingPreset.PACING_PRESET_UNSPECIFIED:
      return "PACING_PRESET_UNSPECIFIED";
    case PacingPreset.PACING_PRESET_SLOW:
      return "PACING_PRESET_SLOW";
    case PacingPreset.PACING_PRESET_MODERATE:
      return "PACING_PRESET_MODERATE";
    case PacingPreset.PACING_PRESET_FAST:
      return "PACING_PRESET_FAST";
    case PacingPreset.PACING_PRESET_DYNAMIC:
      return "PACING_PRESET_DYNAMIC";
    case PacingPreset.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MatchStrategy {
  MATCH_STRATEGY_UNSPECIFIED = 0,
  MATCH_STRATEGY_KEYWORD = 1,
  MATCH_STRATEGY_EMBEDDING = 2,
  MATCH_STRATEGY_HYBRID = 3,
  UNRECOGNIZED = -1,
}

export function matchStrategyFromJSON(object: any): MatchStrategy {
  switch (object) {
    case 0:
    case "MATCH_STRATEGY_UNSPECIFIED":
      return MatchStrategy.MATCH_STRATEGY_UNSPECIFIED;
    case 1:
    case "MATCH_STRATEGY_KEYWORD":
      return MatchStrategy.MATCH_STRATEGY_KEYWORD;
    case 2:
    case "MATCH_STRATEGY_EMBEDDING":
      return MatchStrategy.MATCH_STRATEGY_EMBEDDING;
    case 3:
    case "MATCH_STRATEGY_HYBRID":
      return MatchStrategy.MATCH_STRATEGY_HYBRID;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MatchStrategy.UNRECOGNIZED;
  }
}

export function matchStrategyToJSON(object: MatchStrategy): string {
  switch (object) {
    case MatchStrategy.MATCH_STRATEGY_UNSPECIFIED:
      return "MATCH_STRATEGY_UNSPECIFIED";
    case MatchStrategy.MATCH_STRATEGY_KEYWORD:
      return "MATCH_STRATEGY_KEYWORD";
    case MatchStrategy.MATCH_STRATEGY_EMBEDDING:
      return "MATCH_STRATEGY_EMBEDDING";
    case MatchStrategy.MATCH_STRATEGY_HYBRID:
      return "MATCH_STRATEGY_HYBRID";
    case MatchStrategy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ParseScriptRequest {
  text?: string | undefined;
  filePath?:
    | string
    | undefined;
  /** "screenplay", "youtube", "podcast", "narration" */
  formatHint: string;
}

export interface ParseScriptResponse {
  segments: ScriptSegment[];
  metadata: ScriptMetadata | undefined;
}

export interface ScriptSegment {
  index: number;
  type: SegmentType;
  content: string;
  speaker: string;
  sceneDescription: string;
  visualDirection: string;
  audioDirection: string;
  estimatedDurationSeconds: number;
  /** keywords for asset matching */
  assetHints: string[];
}

export interface ScriptMetadata {
  title: string;
  format: string;
  estimatedTotalDurationSeconds: number;
  segmentCount: number;
}

export interface GenerateEDLRequest {
  segments: ScriptSegment[];
  availableAssets: Asset[];
  matches: AssetMatch[];
  settings: EDLSettings | undefined;
}

export interface EDLSettings {
  resolution: Resolution | undefined;
  frameRate: number;
  /** e.g., "cross_dissolve" */
  defaultTransition: string;
  defaultTransitionDuration: number;
  pacing: PacingPreset;
}

export interface GenerateEDLResponse {
  edl: EditDecisionList | undefined;
  warnings: string[];
}

export interface MatchAssetsRequest {
  segments: ScriptSegment[];
  availableAssets: Asset[];
  strategy: MatchStrategy;
}

export interface MatchAssetsResponse {
  matches: AssetMatch[];
  unmatched: UnmatchedSegment[];
}

export interface AssetMatch {
  segmentIndex: number;
  assetId: string;
  confidence: number;
  reasoning: string;
  suggestedRange: TimeRange | undefined;
}

export interface UnmatchedSegment {
  segmentIndex: number;
  reason: string;
  suggestions: string[];
}

export interface AnalyzePacingRequest {
  edl:
    | EditDecisionList
    | undefined;
  /** "energetic", "calm", "dramatic" */
  targetMood: string;
}

export interface AnalyzePacingResponse {
  adjustments: PacingAdjustment[];
  currentAvgClipDuration: number;
  suggestedAvgClipDuration: number;
}

export interface PacingAdjustment {
  edlEntryIndex: number;
  currentDuration: number;
  suggestedDuration: number;
  reason: string;
}

function createBaseParseScriptRequest(): ParseScriptRequest {
  return { text: undefined, filePath: undefined, formatHint: "" };
}

export const ParseScriptRequest: MessageFns<ParseScriptRequest> = {
  encode(message: ParseScriptRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== undefined) {
      writer.uint32(10).string(message.text);
    }
    if (message.filePath !== undefined) {
      writer.uint32(18).string(message.filePath);
    }
    if (message.formatHint !== "") {
      writer.uint32(26).string(message.formatHint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParseScriptRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParseScriptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.formatHint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParseScriptRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : undefined,
      filePath: isSet(object.filePath)
        ? globalThis.String(object.filePath)
        : isSet(object.file_path)
        ? globalThis.String(object.file_path)
        : undefined,
      formatHint: isSet(object.formatHint)
        ? globalThis.String(object.formatHint)
        : isSet(object.format_hint)
        ? globalThis.String(object.format_hint)
        : "",
    };
  },

  toJSON(message: ParseScriptRequest): unknown {
    const obj: any = {};
    if (message.text !== undefined) {
      obj.text = message.text;
    }
    if (message.filePath !== undefined) {
      obj.filePath = message.filePath;
    }
    if (message.formatHint !== "") {
      obj.formatHint = message.formatHint;
    }
    return obj;
  },

  create(base?: DeepPartial<ParseScriptRequest>): ParseScriptRequest {
    return ParseScriptRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ParseScriptRequest>): ParseScriptRequest {
    const message = createBaseParseScriptRequest();
    message.text = object.text ?? undefined;
    message.filePath = object.filePath ?? undefined;
    message.formatHint = object.formatHint ?? "";
    return message;
  },
};

function createBaseParseScriptResponse(): ParseScriptResponse {
  return { segments: [], metadata: undefined };
}

export const ParseScriptResponse: MessageFns<ParseScriptResponse> = {
  encode(message: ParseScriptResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.segments) {
      ScriptSegment.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.metadata !== undefined) {
      ScriptMetadata.encode(message.metadata, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParseScriptResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParseScriptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.segments.push(ScriptSegment.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.metadata = ScriptMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParseScriptResponse {
    return {
      segments: globalThis.Array.isArray(object?.segments)
        ? object.segments.map((e: any) => ScriptSegment.fromJSON(e))
        : [],
      metadata: isSet(object.metadata) ? ScriptMetadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ParseScriptResponse): unknown {
    const obj: any = {};
    if (message.segments?.length) {
      obj.segments = message.segments.map((e) => ScriptSegment.toJSON(e));
    }
    if (message.metadata !== undefined) {
      obj.metadata = ScriptMetadata.toJSON(message.metadata);
    }
    return obj;
  },

  create(base?: DeepPartial<ParseScriptResponse>): ParseScriptResponse {
    return ParseScriptResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ParseScriptResponse>): ParseScriptResponse {
    const message = createBaseParseScriptResponse();
    message.segments = object.segments?.map((e) => ScriptSegment.fromPartial(e)) || [];
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? ScriptMetadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseScriptSegment(): ScriptSegment {
  return {
    index: 0,
    type: 0,
    content: "",
    speaker: "",
    sceneDescription: "",
    visualDirection: "",
    audioDirection: "",
    estimatedDurationSeconds: 0,
    assetHints: [],
  };
}

export const ScriptSegment: MessageFns<ScriptSegment> = {
  encode(message: ScriptSegment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    if (message.speaker !== "") {
      writer.uint32(34).string(message.speaker);
    }
    if (message.sceneDescription !== "") {
      writer.uint32(42).string(message.sceneDescription);
    }
    if (message.visualDirection !== "") {
      writer.uint32(50).string(message.visualDirection);
    }
    if (message.audioDirection !== "") {
      writer.uint32(58).string(message.audioDirection);
    }
    if (message.estimatedDurationSeconds !== 0) {
      writer.uint32(65).double(message.estimatedDurationSeconds);
    }
    for (const v of message.assetHints) {
      writer.uint32(74).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScriptSegment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScriptSegment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.speaker = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sceneDescription = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.visualDirection = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.audioDirection = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 65) {
            break;
          }

          message.estimatedDurationSeconds = reader.double();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.assetHints.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScriptSegment {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      type: isSet(object.type) ? segmentTypeFromJSON(object.type) : 0,
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      speaker: isSet(object.speaker) ? globalThis.String(object.speaker) : "",
      sceneDescription: isSet(object.sceneDescription)
        ? globalThis.String(object.sceneDescription)
        : isSet(object.scene_description)
        ? globalThis.String(object.scene_description)
        : "",
      visualDirection: isSet(object.visualDirection)
        ? globalThis.String(object.visualDirection)
        : isSet(object.visual_direction)
        ? globalThis.String(object.visual_direction)
        : "",
      audioDirection: isSet(object.audioDirection)
        ? globalThis.String(object.audioDirection)
        : isSet(object.audio_direction)
        ? globalThis.String(object.audio_direction)
        : "",
      estimatedDurationSeconds: isSet(object.estimatedDurationSeconds)
        ? globalThis.Number(object.estimatedDurationSeconds)
        : isSet(object.estimated_duration_seconds)
        ? globalThis.Number(object.estimated_duration_seconds)
        : 0,
      assetHints: globalThis.Array.isArray(object?.assetHints)
        ? object.assetHints.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.asset_hints)
        ? object.asset_hints.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ScriptSegment): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.type !== 0) {
      obj.type = segmentTypeToJSON(message.type);
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.speaker !== "") {
      obj.speaker = message.speaker;
    }
    if (message.sceneDescription !== "") {
      obj.sceneDescription = message.sceneDescription;
    }
    if (message.visualDirection !== "") {
      obj.visualDirection = message.visualDirection;
    }
    if (message.audioDirection !== "") {
      obj.audioDirection = message.audioDirection;
    }
    if (message.estimatedDurationSeconds !== 0) {
      obj.estimatedDurationSeconds = message.estimatedDurationSeconds;
    }
    if (message.assetHints?.length) {
      obj.assetHints = message.assetHints;
    }
    return obj;
  },

  create(base?: DeepPartial<ScriptSegment>): ScriptSegment {
    return ScriptSegment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScriptSegment>): ScriptSegment {
    const message = createBaseScriptSegment();
    message.index = object.index ?? 0;
    message.type = object.type ?? 0;
    message.content = object.content ?? "";
    message.speaker = object.speaker ?? "";
    message.sceneDescription = object.sceneDescription ?? "";
    message.visualDirection = object.visualDirection ?? "";
    message.audioDirection = object.audioDirection ?? "";
    message.estimatedDurationSeconds = object.estimatedDurationSeconds ?? 0;
    message.assetHints = object.assetHints?.map((e) => e) || [];
    return message;
  },
};

function createBaseScriptMetadata(): ScriptMetadata {
  return { title: "", format: "", estimatedTotalDurationSeconds: 0, segmentCount: 0 };
}

export const ScriptMetadata: MessageFns<ScriptMetadata> = {
  encode(message: ScriptMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.format !== "") {
      writer.uint32(18).string(message.format);
    }
    if (message.estimatedTotalDurationSeconds !== 0) {
      writer.uint32(25).double(message.estimatedTotalDurationSeconds);
    }
    if (message.segmentCount !== 0) {
      writer.uint32(32).uint32(message.segmentCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScriptMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScriptMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.format = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.estimatedTotalDurationSeconds = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.segmentCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScriptMetadata {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      format: isSet(object.format) ? globalThis.String(object.format) : "",
      estimatedTotalDurationSeconds: isSet(object.estimatedTotalDurationSeconds)
        ? globalThis.Number(object.estimatedTotalDurationSeconds)
        : isSet(object.estimated_total_duration_seconds)
        ? globalThis.Number(object.estimated_total_duration_seconds)
        : 0,
      segmentCount: isSet(object.segmentCount)
        ? globalThis.Number(object.segmentCount)
        : isSet(object.segment_count)
        ? globalThis.Number(object.segment_count)
        : 0,
    };
  },

  toJSON(message: ScriptMetadata): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.format !== "") {
      obj.format = message.format;
    }
    if (message.estimatedTotalDurationSeconds !== 0) {
      obj.estimatedTotalDurationSeconds = message.estimatedTotalDurationSeconds;
    }
    if (message.segmentCount !== 0) {
      obj.segmentCount = Math.round(message.segmentCount);
    }
    return obj;
  },

  create(base?: DeepPartial<ScriptMetadata>): ScriptMetadata {
    return ScriptMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScriptMetadata>): ScriptMetadata {
    const message = createBaseScriptMetadata();
    message.title = object.title ?? "";
    message.format = object.format ?? "";
    message.estimatedTotalDurationSeconds = object.estimatedTotalDurationSeconds ?? 0;
    message.segmentCount = object.segmentCount ?? 0;
    return message;
  },
};

function createBaseGenerateEDLRequest(): GenerateEDLRequest {
  return { segments: [], availableAssets: [], matches: [], settings: undefined };
}

export const GenerateEDLRequest: MessageFns<GenerateEDLRequest> = {
  encode(message: GenerateEDLRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.segments) {
      ScriptSegment.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.availableAssets) {
      Asset.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.matches) {
      AssetMatch.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.settings !== undefined) {
      EDLSettings.encode(message.settings, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateEDLRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateEDLRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.segments.push(ScriptSegment.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.availableAssets.push(Asset.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.matches.push(AssetMatch.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.settings = EDLSettings.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateEDLRequest {
    return {
      segments: globalThis.Array.isArray(object?.segments)
        ? object.segments.map((e: any) => ScriptSegment.fromJSON(e))
        : [],
      availableAssets: globalThis.Array.isArray(object?.availableAssets)
        ? object.availableAssets.map((e: any) => Asset.fromJSON(e))
        : globalThis.Array.isArray(object?.available_assets)
        ? object.available_assets.map((e: any) => Asset.fromJSON(e))
        : [],
      matches: globalThis.Array.isArray(object?.matches) ? object.matches.map((e: any) => AssetMatch.fromJSON(e)) : [],
      settings: isSet(object.settings) ? EDLSettings.fromJSON(object.settings) : undefined,
    };
  },

  toJSON(message: GenerateEDLRequest): unknown {
    const obj: any = {};
    if (message.segments?.length) {
      obj.segments = message.segments.map((e) => ScriptSegment.toJSON(e));
    }
    if (message.availableAssets?.length) {
      obj.availableAssets = message.availableAssets.map((e) => Asset.toJSON(e));
    }
    if (message.matches?.length) {
      obj.matches = message.matches.map((e) => AssetMatch.toJSON(e));
    }
    if (message.settings !== undefined) {
      obj.settings = EDLSettings.toJSON(message.settings);
    }
    return obj;
  },

  create(base?: DeepPartial<GenerateEDLRequest>): GenerateEDLRequest {
    return GenerateEDLRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenerateEDLRequest>): GenerateEDLRequest {
    const message = createBaseGenerateEDLRequest();
    message.segments = object.segments?.map((e) => ScriptSegment.fromPartial(e)) || [];
    message.availableAssets = object.availableAssets?.map((e) => Asset.fromPartial(e)) || [];
    message.matches = object.matches?.map((e) => AssetMatch.fromPartial(e)) || [];
    message.settings = (object.settings !== undefined && object.settings !== null)
      ? EDLSettings.fromPartial(object.settings)
      : undefined;
    return message;
  },
};

function createBaseEDLSettings(): EDLSettings {
  return { resolution: undefined, frameRate: 0, defaultTransition: "", defaultTransitionDuration: 0, pacing: 0 };
}

export const EDLSettings: MessageFns<EDLSettings> = {
  encode(message: EDLSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resolution !== undefined) {
      Resolution.encode(message.resolution, writer.uint32(10).fork()).join();
    }
    if (message.frameRate !== 0) {
      writer.uint32(17).double(message.frameRate);
    }
    if (message.defaultTransition !== "") {
      writer.uint32(26).string(message.defaultTransition);
    }
    if (message.defaultTransitionDuration !== 0) {
      writer.uint32(33).double(message.defaultTransitionDuration);
    }
    if (message.pacing !== 0) {
      writer.uint32(40).int32(message.pacing);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EDLSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEDLSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resolution = Resolution.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.frameRate = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.defaultTransition = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.defaultTransitionDuration = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pacing = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EDLSettings {
    return {
      resolution: isSet(object.resolution) ? Resolution.fromJSON(object.resolution) : undefined,
      frameRate: isSet(object.frameRate)
        ? globalThis.Number(object.frameRate)
        : isSet(object.frame_rate)
        ? globalThis.Number(object.frame_rate)
        : 0,
      defaultTransition: isSet(object.defaultTransition)
        ? globalThis.String(object.defaultTransition)
        : isSet(object.default_transition)
        ? globalThis.String(object.default_transition)
        : "",
      defaultTransitionDuration: isSet(object.defaultTransitionDuration)
        ? globalThis.Number(object.defaultTransitionDuration)
        : isSet(object.default_transition_duration)
        ? globalThis.Number(object.default_transition_duration)
        : 0,
      pacing: isSet(object.pacing) ? pacingPresetFromJSON(object.pacing) : 0,
    };
  },

  toJSON(message: EDLSettings): unknown {
    const obj: any = {};
    if (message.resolution !== undefined) {
      obj.resolution = Resolution.toJSON(message.resolution);
    }
    if (message.frameRate !== 0) {
      obj.frameRate = message.frameRate;
    }
    if (message.defaultTransition !== "") {
      obj.defaultTransition = message.defaultTransition;
    }
    if (message.defaultTransitionDuration !== 0) {
      obj.defaultTransitionDuration = message.defaultTransitionDuration;
    }
    if (message.pacing !== 0) {
      obj.pacing = pacingPresetToJSON(message.pacing);
    }
    return obj;
  },

  create(base?: DeepPartial<EDLSettings>): EDLSettings {
    return EDLSettings.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EDLSettings>): EDLSettings {
    const message = createBaseEDLSettings();
    message.resolution = (object.resolution !== undefined && object.resolution !== null)
      ? Resolution.fromPartial(object.resolution)
      : undefined;
    message.frameRate = object.frameRate ?? 0;
    message.defaultTransition = object.defaultTransition ?? "";
    message.defaultTransitionDuration = object.defaultTransitionDuration ?? 0;
    message.pacing = object.pacing ?? 0;
    return message;
  },
};

function createBaseGenerateEDLResponse(): GenerateEDLResponse {
  return { edl: undefined, warnings: [] };
}

export const GenerateEDLResponse: MessageFns<GenerateEDLResponse> = {
  encode(message: GenerateEDLResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.edl !== undefined) {
      EditDecisionList.encode(message.edl, writer.uint32(10).fork()).join();
    }
    for (const v of message.warnings) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateEDLResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateEDLResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edl = EditDecisionList.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateEDLResponse {
    return {
      edl: isSet(object.edl) ? EditDecisionList.fromJSON(object.edl) : undefined,
      warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GenerateEDLResponse): unknown {
    const obj: any = {};
    if (message.edl !== undefined) {
      obj.edl = EditDecisionList.toJSON(message.edl);
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    return obj;
  },

  create(base?: DeepPartial<GenerateEDLResponse>): GenerateEDLResponse {
    return GenerateEDLResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenerateEDLResponse>): GenerateEDLResponse {
    const message = createBaseGenerateEDLResponse();
    message.edl = (object.edl !== undefined && object.edl !== null)
      ? EditDecisionList.fromPartial(object.edl)
      : undefined;
    message.warnings = object.warnings?.map((e) => e) || [];
    return message;
  },
};

function createBaseMatchAssetsRequest(): MatchAssetsRequest {
  return { segments: [], availableAssets: [], strategy: 0 };
}

export const MatchAssetsRequest: MessageFns<MatchAssetsRequest> = {
  encode(message: MatchAssetsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.segments) {
      ScriptSegment.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.availableAssets) {
      Asset.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.strategy !== 0) {
      writer.uint32(24).int32(message.strategy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MatchAssetsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMatchAssetsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.segments.push(ScriptSegment.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.availableAssets.push(Asset.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.strategy = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MatchAssetsRequest {
    return {
      segments: globalThis.Array.isArray(object?.segments)
        ? object.segments.map((e: any) => ScriptSegment.fromJSON(e))
        : [],
      availableAssets: globalThis.Array.isArray(object?.availableAssets)
        ? object.availableAssets.map((e: any) => Asset.fromJSON(e))
        : globalThis.Array.isArray(object?.available_assets)
        ? object.available_assets.map((e: any) => Asset.fromJSON(e))
        : [],
      strategy: isSet(object.strategy) ? matchStrategyFromJSON(object.strategy) : 0,
    };
  },

  toJSON(message: MatchAssetsRequest): unknown {
    const obj: any = {};
    if (message.segments?.length) {
      obj.segments = message.segments.map((e) => ScriptSegment.toJSON(e));
    }
    if (message.availableAssets?.length) {
      obj.availableAssets = message.availableAssets.map((e) => Asset.toJSON(e));
    }
    if (message.strategy !== 0) {
      obj.strategy = matchStrategyToJSON(message.strategy);
    }
    return obj;
  },

  create(base?: DeepPartial<MatchAssetsRequest>): MatchAssetsRequest {
    return MatchAssetsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MatchAssetsRequest>): MatchAssetsRequest {
    const message = createBaseMatchAssetsRequest();
    message.segments = object.segments?.map((e) => ScriptSegment.fromPartial(e)) || [];
    message.availableAssets = object.availableAssets?.map((e) => Asset.fromPartial(e)) || [];
    message.strategy = object.strategy ?? 0;
    return message;
  },
};

function createBaseMatchAssetsResponse(): MatchAssetsResponse {
  return { matches: [], unmatched: [] };
}

export const MatchAssetsResponse: MessageFns<MatchAssetsResponse> = {
  encode(message: MatchAssetsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.matches) {
      AssetMatch.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.unmatched) {
      UnmatchedSegment.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MatchAssetsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMatchAssetsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.matches.push(AssetMatch.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.unmatched.push(UnmatchedSegment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MatchAssetsResponse {
    return {
      matches: globalThis.Array.isArray(object?.matches) ? object.matches.map((e: any) => AssetMatch.fromJSON(e)) : [],
      unmatched: globalThis.Array.isArray(object?.unmatched)
        ? object.unmatched.map((e: any) => UnmatchedSegment.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MatchAssetsResponse): unknown {
    const obj: any = {};
    if (message.matches?.length) {
      obj.matches = message.matches.map((e) => AssetMatch.toJSON(e));
    }
    if (message.unmatched?.length) {
      obj.unmatched = message.unmatched.map((e) => UnmatchedSegment.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MatchAssetsResponse>): MatchAssetsResponse {
    return MatchAssetsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MatchAssetsResponse>): MatchAssetsResponse {
    const message = createBaseMatchAssetsResponse();
    message.matches = object.matches?.map((e) => AssetMatch.fromPartial(e)) || [];
    message.unmatched = object.unmatched?.map((e) => UnmatchedSegment.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAssetMatch(): AssetMatch {
  return { segmentIndex: 0, assetId: "", confidence: 0, reasoning: "", suggestedRange: undefined };
}

export const AssetMatch: MessageFns<AssetMatch> = {
  encode(message: AssetMatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.segmentIndex !== 0) {
      writer.uint32(8).uint32(message.segmentIndex);
    }
    if (message.assetId !== "") {
      writer.uint32(18).string(message.assetId);
    }
    if (message.confidence !== 0) {
      writer.uint32(25).double(message.confidence);
    }
    if (message.reasoning !== "") {
      writer.uint32(34).string(message.reasoning);
    }
    if (message.suggestedRange !== undefined) {
      TimeRange.encode(message.suggestedRange, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssetMatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssetMatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.segmentIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.assetId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.confidence = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reasoning = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.suggestedRange = TimeRange.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AssetMatch {
    return {
      segmentIndex: isSet(object.segmentIndex)
        ? globalThis.Number(object.segmentIndex)
        : isSet(object.segment_index)
        ? globalThis.Number(object.segment_index)
        : 0,
      assetId: isSet(object.assetId)
        ? globalThis.String(object.assetId)
        : isSet(object.asset_id)
        ? globalThis.String(object.asset_id)
        : "",
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      reasoning: isSet(object.reasoning) ? globalThis.String(object.reasoning) : "",
      suggestedRange: isSet(object.suggestedRange)
        ? TimeRange.fromJSON(object.suggestedRange)
        : isSet(object.suggested_range)
        ? TimeRange.fromJSON(object.suggested_range)
        : undefined,
    };
  },

  toJSON(message: AssetMatch): unknown {
    const obj: any = {};
    if (message.segmentIndex !== 0) {
      obj.segmentIndex = Math.round(message.segmentIndex);
    }
    if (message.assetId !== "") {
      obj.assetId = message.assetId;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.reasoning !== "") {
      obj.reasoning = message.reasoning;
    }
    if (message.suggestedRange !== undefined) {
      obj.suggestedRange = TimeRange.toJSON(message.suggestedRange);
    }
    return obj;
  },

  create(base?: DeepPartial<AssetMatch>): AssetMatch {
    return AssetMatch.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AssetMatch>): AssetMatch {
    const message = createBaseAssetMatch();
    message.segmentIndex = object.segmentIndex ?? 0;
    message.assetId = object.assetId ?? "";
    message.confidence = object.confidence ?? 0;
    message.reasoning = object.reasoning ?? "";
    message.suggestedRange = (object.suggestedRange !== undefined && object.suggestedRange !== null)
      ? TimeRange.fromPartial(object.suggestedRange)
      : undefined;
    return message;
  },
};

function createBaseUnmatchedSegment(): UnmatchedSegment {
  return { segmentIndex: 0, reason: "", suggestions: [] };
}

export const UnmatchedSegment: MessageFns<UnmatchedSegment> = {
  encode(message: UnmatchedSegment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.segmentIndex !== 0) {
      writer.uint32(8).uint32(message.segmentIndex);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    for (const v of message.suggestions) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnmatchedSegment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnmatchedSegment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.segmentIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.suggestions.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnmatchedSegment {
    return {
      segmentIndex: isSet(object.segmentIndex)
        ? globalThis.Number(object.segmentIndex)
        : isSet(object.segment_index)
        ? globalThis.Number(object.segment_index)
        : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      suggestions: globalThis.Array.isArray(object?.suggestions)
        ? object.suggestions.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: UnmatchedSegment): unknown {
    const obj: any = {};
    if (message.segmentIndex !== 0) {
      obj.segmentIndex = Math.round(message.segmentIndex);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.suggestions?.length) {
      obj.suggestions = message.suggestions;
    }
    return obj;
  },

  create(base?: DeepPartial<UnmatchedSegment>): UnmatchedSegment {
    return UnmatchedSegment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UnmatchedSegment>): UnmatchedSegment {
    const message = createBaseUnmatchedSegment();
    message.segmentIndex = object.segmentIndex ?? 0;
    message.reason = object.reason ?? "";
    message.suggestions = object.suggestions?.map((e) => e) || [];
    return message;
  },
};

function createBaseAnalyzePacingRequest(): AnalyzePacingRequest {
  return { edl: undefined, targetMood: "" };
}

export const AnalyzePacingRequest: MessageFns<AnalyzePacingRequest> = {
  encode(message: AnalyzePacingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.edl !== undefined) {
      EditDecisionList.encode(message.edl, writer.uint32(10).fork()).join();
    }
    if (message.targetMood !== "") {
      writer.uint32(18).string(message.targetMood);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzePacingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyzePacingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.edl = EditDecisionList.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.targetMood = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyzePacingRequest {
    return {
      edl: isSet(object.edl) ? EditDecisionList.fromJSON(object.edl) : undefined,
      targetMood: isSet(object.targetMood)
        ? globalThis.String(object.targetMood)
        : isSet(object.target_mood)
        ? globalThis.String(object.target_mood)
        : "",
    };
  },

  toJSON(message: AnalyzePacingRequest): unknown {
    const obj: any = {};
    if (message.edl !== undefined) {
      obj.edl = EditDecisionList.toJSON(message.edl);
    }
    if (message.targetMood !== "") {
      obj.targetMood = message.targetMood;
    }
    return obj;
  },

  create(base?: DeepPartial<AnalyzePacingRequest>): AnalyzePacingRequest {
    return AnalyzePacingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalyzePacingRequest>): AnalyzePacingRequest {
    const message = createBaseAnalyzePacingRequest();
    message.edl = (object.edl !== undefined && object.edl !== null)
      ? EditDecisionList.fromPartial(object.edl)
      : undefined;
    message.targetMood = object.targetMood ?? "";
    return message;
  },
};

function createBaseAnalyzePacingResponse(): AnalyzePacingResponse {
  return { adjustments: [], currentAvgClipDuration: 0, suggestedAvgClipDuration: 0 };
}

export const AnalyzePacingResponse: MessageFns<AnalyzePacingResponse> = {
  encode(message: AnalyzePacingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.adjustments) {
      PacingAdjustment.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.currentAvgClipDuration !== 0) {
      writer.uint32(17).double(message.currentAvgClipDuration);
    }
    if (message.suggestedAvgClipDuration !== 0) {
      writer.uint32(25).double(message.suggestedAvgClipDuration);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzePacingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyzePacingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.adjustments.push(PacingAdjustment.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.currentAvgClipDuration = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.suggestedAvgClipDuration = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyzePacingResponse {
    return {
      adjustments: globalThis.Array.isArray(object?.adjustments)
        ? object.adjustments.map((e: any) => PacingAdjustment.fromJSON(e))
        : [],
      currentAvgClipDuration: isSet(object.currentAvgClipDuration)
        ? globalThis.Number(object.currentAvgClipDuration)
        : isSet(object.current_avg_clip_duration)
        ? globalThis.Number(object.current_avg_clip_duration)
        : 0,
      suggestedAvgClipDuration: isSet(object.suggestedAvgClipDuration)
        ? globalThis.Number(object.suggestedAvgClipDuration)
        : isSet(object.suggested_avg_clip_duration)
        ? globalThis.Number(object.suggested_avg_clip_duration)
        : 0,
    };
  },

  toJSON(message: AnalyzePacingResponse): unknown {
    const obj: any = {};
    if (message.adjustments?.length) {
      obj.adjustments = message.adjustments.map((e) => PacingAdjustment.toJSON(e));
    }
    if (message.currentAvgClipDuration !== 0) {
      obj.currentAvgClipDuration = message.currentAvgClipDuration;
    }
    if (message.suggestedAvgClipDuration !== 0) {
      obj.suggestedAvgClipDuration = message.suggestedAvgClipDuration;
    }
    return obj;
  },

  create(base?: DeepPartial<AnalyzePacingResponse>): AnalyzePacingResponse {
    return AnalyzePacingResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalyzePacingResponse>): AnalyzePacingResponse {
    const message = createBaseAnalyzePacingResponse();
    message.adjustments = object.adjustments?.map((e) => PacingAdjustment.fromPartial(e)) || [];
    message.currentAvgClipDuration = object.currentAvgClipDuration ?? 0;
    message.suggestedAvgClipDuration = object.suggestedAvgClipDuration ?? 0;
    return message;
  },
};

function createBasePacingAdjustment(): PacingAdjustment {
  return { edlEntryIndex: 0, currentDuration: 0, suggestedDuration: 0, reason: "" };
}

export const PacingAdjustment: MessageFns<PacingAdjustment> = {
  encode(message: PacingAdjustment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.edlEntryIndex !== 0) {
      writer.uint32(8).uint32(message.edlEntryIndex);
    }
    if (message.currentDuration !== 0) {
      writer.uint32(17).double(message.currentDuration);
    }
    if (message.suggestedDuration !== 0) {
      writer.uint32(25).double(message.suggestedDuration);
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PacingAdjustment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePacingAdjustment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.edlEntryIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.currentDuration = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.suggestedDuration = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PacingAdjustment {
    return {
      edlEntryIndex: isSet(object.edlEntryIndex)
        ? globalThis.Number(object.edlEntryIndex)
        : isSet(object.edl_entry_index)
        ? globalThis.Number(object.edl_entry_index)
        : 0,
      currentDuration: isSet(object.currentDuration)
        ? globalThis.Number(object.currentDuration)
        : isSet(object.current_duration)
        ? globalThis.Number(object.current_duration)
        : 0,
      suggestedDuration: isSet(object.suggestedDuration)
        ? globalThis.Number(object.suggestedDuration)
        : isSet(object.suggested_duration)
        ? globalThis.Number(object.suggested_duration)
        : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: PacingAdjustment): unknown {
    const obj: any = {};
    if (message.edlEntryIndex !== 0) {
      obj.edlEntryIndex = Math.round(message.edlEntryIndex);
    }
    if (message.currentDuration !== 0) {
      obj.currentDuration = message.currentDuration;
    }
    if (message.suggestedDuration !== 0) {
      obj.suggestedDuration = message.suggestedDuration;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<PacingAdjustment>): PacingAdjustment {
    return PacingAdjustment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PacingAdjustment>): PacingAdjustment {
    const message = createBasePacingAdjustment();
    message.edlEntryIndex = object.edlEntryIndex ?? 0;
    message.currentDuration = object.currentDuration ?? 0;
    message.suggestedDuration = object.suggestedDuration ?? 0;
    message.reason = object.reason ?? "";
    return message;
  },
};

/**
 * Intelligence handles script parsing, EDL generation, and AI-powered asset matching.
 * Implemented by the Python service.
 */
export type IntelligenceServiceDefinition = typeof IntelligenceServiceDefinition;
export const IntelligenceServiceDefinition = {
  name: "IntelligenceService",
  fullName: "premierpro.intelligence.v1.IntelligenceService",
  methods: {
    /** Parse a script (text or file) into structured segments. */
    parseScript: {
      name: "ParseScript",
      requestType: ParseScriptRequest,
      requestStream: false,
      responseType: ParseScriptResponse,
      responseStream: false,
      options: {},
    },
    /** Generate an Edit Decision List from a parsed script and available assets. */
    generateEDL: {
      name: "GenerateEDL",
      requestType: GenerateEDLRequest,
      requestStream: false,
      responseType: GenerateEDLResponse,
      responseStream: false,
      options: {},
    },
    /** Match script segments to the best available assets. */
    matchAssets: {
      name: "MatchAssets",
      requestType: MatchAssetsRequest,
      requestStream: false,
      responseType: MatchAssetsResponse,
      responseStream: false,
      options: {},
    },
    /** Analyze pacing and suggest timing adjustments. */
    analyzePacing: {
      name: "AnalyzePacing",
      requestType: AnalyzePacingRequest,
      requestStream: false,
      responseType: AnalyzePacingResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface IntelligenceServiceImplementation<CallContextExt = {}> {
  /** Parse a script (text or file) into structured segments. */
  parseScript(
    request: ParseScriptRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ParseScriptResponse>>;
  /** Generate an Edit Decision List from a parsed script and available assets. */
  generateEDL(
    request: GenerateEDLRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GenerateEDLResponse>>;
  /** Match script segments to the best available assets. */
  matchAssets(
    request: MatchAssetsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<MatchAssetsResponse>>;
  /** Analyze pacing and suggest timing adjustments. */
  analyzePacing(
    request: AnalyzePacingRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<AnalyzePacingResponse>>;
}

export interface IntelligenceServiceClient<CallOptionsExt = {}> {
  /** Parse a script (text or file) into structured segments. */
  parseScript(
    request: DeepPartial<ParseScriptRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ParseScriptResponse>;
  /** Generate an Edit Decision List from a parsed script and available assets. */
  generateEDL(
    request: DeepPartial<GenerateEDLRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GenerateEDLResponse>;
  /** Match script segments to the best available assets. */
  matchAssets(
    request: DeepPartial<MatchAssetsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<MatchAssetsResponse>;
  /** Analyze pacing and suggest timing adjustments. */
  analyzePacing(
    request: DeepPartial<AnalyzePacingRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<AnalyzePacingResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
